*** Begin Patch
*** Update File: docs/SL_gap_n1_O3a_phase_rigidity_proof.tex
@@
-于 $0<c<1/2$ 由初等解析估计 (含 $\partial_qM_2<0$ 在 $D$ 上的完全解析证明,
-C4 区间段 $K>0$ 的完全解析证明, 以及 $J_1^{(2)}$ 在真曲线区域上的完全解析
-下界, 见定理 \ref{thm:j1e1}) 结合一处紧盒导数符号证书证明: 该证书先把相位
-方程显式代入, 化为二维显式函数 $J_2^{(2)}$ 在盒
-$[0.655,1.0472]\times[1,2]$ 上的完全解析上界 (W-分解链, 见定理
-\ref{thm:j2e1}), 不再夹取隐式相位根; 其余参数区域严格为负, 故对称线上恰有一个
-good root。全文把\emph{严格解析证明}、\emph{有限区间证书}与\emph{普通数值
-扫描}三类证据明确区分并逐一标注: 证明链只依赖前两类, 其中证书仅用于引理
-\ref{thm:keylemma} 的伴随命题 $(LOG)$; 二维参数化目标 (I3) 的 $J_2^{(2)}$ 侧
-已完全解析化 (定理 \ref{thm:j2e1}), 不再依赖证书。数值扫描仅用于交叉检验,
-不作为结论依据。证书重放器不是形式化证明助手内验证过的内核, 本文如实标注为
-\emph{独立审计、证书支持的严格证明}。
+于 $0<c<1/2$ 由完全初等解析估计证明: 含 $\partial_qM_2<0$ 在 $D$ 上的完全解析
+证明、C4 区间段 $K>0$ 的完全解析证明、$J_1^{(2)}$ 在真曲线区域上的完全解析
+下界 (定理 \ref{thm:j1e1}) 与 $J_2^{(2)}$ 在盒 $[0.655,1.0472]\times[1,2]$
+上的完全解析上界 (W-分解链, 定理 \ref{thm:j2e1}), 不再夹取隐式相位根; 其余
+参数区域严格为负, 故对称线上恰有一个 good root。伴随命题 $(LOG)$
+( $\frac{d}{dc}\log(\widetilde M_{f1}/\widetilde M_{f2})<0$) 由定理
+\ref{thm:LOG} 纯解析证明。全文只使用\emph{严格解析证明} (E1) 一类结论,
+与\emph{普通数值扫描} (E3, 仅交叉检验) 明确区分并逐一标注; 早期版本中的四族
+叶盒证书 (J1 16 叶盒、J2 67 叶盒、C4 200 叶盒与 $(LOG)$ 128 叶盒) 已全部
+解析化移除, 历史与复现合同见第 \ref{sec:certs} 节。
*** End Patch
