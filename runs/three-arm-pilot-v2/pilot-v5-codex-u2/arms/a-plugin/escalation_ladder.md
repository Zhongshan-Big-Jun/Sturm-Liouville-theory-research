# Escalation ladder

- Run ID: `U2-TV-2026-08-26`
- Current cost tier: terminal/stopped at audited partial result.

## Attempts and escalation

- Tier 0: inventory and small structural inspection; result: exact conditional lamp-kernel reduction and endpoint lower-bound route.
- Tier 1: reduce upper bound to a translation gradient for the simple-walk `(minimum,maximum,endpoint)` triple; result: strictly smaller load-bearing obligation `O3a`.
- Tier 1 -> Tier 2/3 trigger: load-bearing range-gradient estimate plus the user's explicit request for research subagents and adversarial audit.
- Tier 2 exact enumeration: range-triple TV computed through `t=150`; produced the exact `H_t^R(A,e)` variation reduction and falsified naive unimodality. This is positive information gain; counting/coupling routes remain live.
- Tier 3 coupling subagent: standard reflection/synchronization route rigorously blocked by an intrinsic logarithmic loss, even under optimal conditional lamp coupling. De-escalation: retain its exact lamp-kernel lemma; concentrate on a smaller range-triple/counting estimate.
- Post-reset Tier 2/3: hash-validated the one-sided and logarithmic partial module; launched only `AVI` and `MC` continuations plus a fresh global audit. Trigger: the two one-sided `12/sqrt(t)` results make `MC` strictly smaller than the original target.
- Terminal: aggregate coarea route returned an exact smaller frontier but no fixed-constant bound; MC continuation produced no mergeable artifact; fresh global audit passed the claimed partial theorem. Stop condition: remaining live routes share the same unproved cancellation obligation and marginal gain reached zero within the wall cap.

## Avoid list

- Do not infer a full-state coupling merely from coalescence of base positions.
- Do not use a local limit theorem without a checked explicit uniform inequality.
- Do not discard parity or the time-zero forced lamps.
- Do not assert unimodality of the exact both-extrema counts in the starting coordinate.
