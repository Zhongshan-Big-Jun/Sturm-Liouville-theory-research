I’m blocked by the workspace tool host: every filesystem command and patch attempt returns `code-mode host is disabled`. I therefore cannot read the required inputs or write `proof.md` without fabricating content.

No files were modified. Please re-enable the execution/filesystem tools and rerun the task.