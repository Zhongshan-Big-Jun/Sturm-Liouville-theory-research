# Research map

- **Exact reductions:** conditional lamp law (`O1`); endpoint projection gives an exact shifted
  binomial TV formula (`O2`).
- **Route outcomes:** path/range coupling (`R-A`) is PARTIAL with a sharp-for-that-coupling
  logarithmic loss; exact state mass transport (`R-C`) is PARTIAL with exact formulas and a
  counterexample to naive sign control; convolution route (`R-B`) returned no artifact and is
  recorded only as INCOMPLETE_RETURN.
- **Unexpected finding:** latent range triples separate the starts more strongly than final
  lamp states, because fair zero lamps conceal visited boundary sites.
- **Strongest theorem:** for every integer `t>=2`, the lower bound is `1/(4sqrt(t))` and the
  upper bound is (5.1), hence `sqrt(2)[3+2log(t+1)]/sqrt(t)`.
- **Failed shortcuts:** the reflected coupling necessarily pays harmonic pre-meeting depth;
  endpoint smoothing alone cannot upper-bound the full law; normalized-range slices are not
  parity-class unimodal.
- **Tools:** exact integer dynamic programming, no network, no random sampling.
- **First open obligation:** `O3`, a fixed-constant full-state `C/sqrt(t)` upper bound.  The
  array estimate `O3c` is one explicit sufficient frontier.
- **Human contribution:** frozen problem statement and restrictions only.
- **Agent contributions:** Route A and Route C files were hash-verified and selectively
  integrated; Route B is an incomplete return with no ingested claim.
- **Avoid list:** do not optimize the reflected two-extreme coupling expecting a fixed
  constant; do not reuse naive unimodality; do not infer anything from the absent Route B file.
