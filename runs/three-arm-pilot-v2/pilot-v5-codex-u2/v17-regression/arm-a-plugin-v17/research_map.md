# Research map

- **Exact reductions:** conditional lamp law (`O1`); endpoint projection gives an exact shifted
  binomial TV formula (`O2`).
- **Live upper-bound routes:** path/range coupling (`R-A`), direct convolution gradient (`R-B`),
  and exact state mass transport (`R-C`).
- **Unexpected finding:** latent range triples separate the starts more strongly than final
  lamp states, because fair zero lamps conceal visited boundary sites.
- **Failed shortcut:** reflected meeting leaves two-extreme coverage; endpoint smoothing alone
  cannot upper-bound the full law.
- **Tools:** exact integer dynamic programming, no network, no random sampling.
- **First open obligation:** `O3`, explicit full-state upper bound.
- **Human contribution:** frozen problem statement and restrictions only.
- **Agent contributions:** pending isolated route artifacts.
