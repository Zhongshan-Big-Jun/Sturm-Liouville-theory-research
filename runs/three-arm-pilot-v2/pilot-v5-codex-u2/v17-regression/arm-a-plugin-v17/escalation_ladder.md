# Escalation ladder

- **Run ID:** `U2-TV-2026-08-27`.
- **Task packet ID:** `FROZEN-U2`.
- **Current cost tier:** 3.

## Attempts

- Tier 0: audited the literal lamp convention and parity; result `O1` exact conditional-lamp
  reduction and the base-projection lower-bound identity.
- Tier 0: reflected coupling attempt; result is the exact unresolved requirement that the
  post-meeting path cover both pre-meeting extrema.
- Tier 1: exact dynamic programming.  Triple law tested through `t=80`; full state through
  `t=12`.  No contradiction to `t^{-1/2}`; range-triple comparison is substantially looser.
  Replay: `python3 reproducibility/exact_small_cases.py --triple-max 80 --full-max 12`.

## Escalations

- Tier 1 -> tier 3: trigger is the user's explicit request for mechanism-distinct research
  subagents, together with the named load-bearing gap `O3` and closure decision `ESCALATE`.
  Three routes have distinct mechanisms and can decide whether the range detour is necessary.

## Avoid list

- Endpoint-only upper bound.
- Equality of entire trajectories.
- Unproved generic heat-kernel gradient theorem.
