# Fresh-context convergence check

## Protocol

This check was reconstructed from files only after route ingestion.  The files read were
`problem_contract.md`, `obligation_graph.md`, `approach_registry.md`, `research_ledger.md`,
`candidate_proof.md`, `research_map.md`, `status_and_literature.md`, `whiteboard.md`,
`closure_gate.md`, `counterexample_log.md`, and `repro_manifest.md`.  Conversational history,
internet sources, prior benchmark outputs, and the absent Route B return were not used as
mathematical evidence.  This pass does not mark any obligation complete.

## Reconstructed state

- The frozen target `O0` requires a fixed explicit `C/sqrt(t)` full-state upper bound.
- Closed and written: literal conditional lamp law for `t>=1` (`O1`), visible-hull sufficiency
  (`O1b`), explicit lower bound with `c=1/4` (`O2`), the logarithmic-loss upper bound (`O3p`),
  and the combined partial theorem (`O5`).
- First open load-bearing obligation: `O3`, the fixed-constant full-state upper bound.
- One exact sufficient frontier is `O3c`, inequality (6.3) for normalized range counts.
- Route A and Route C are PARTIAL; their hashes were verified.  Route B is an
  INCOMPLETE_RETURN and supports no claim.
- Independent completion audit `O4` is unavailable and in any case cannot close while `O3`
  remains open.

## Convergence verdict

`CONVERGING_STRUCTURALLY_BUT_STALLED_AT_FIRST_GAP`.

The run made durable progress: it closed the lower bound, isolated the literal lamp kernel,
proved an explicit `O(log(t)/sqrt(t))` upper bound, produced exact polynomial-size formulas,
and falsified two tempting shortcuts.  It did not converge to the requested fixed-constant
upper bound.  The two completed upper-bound routes share the same harmonic-depth obstruction,
and the third route returned no artifact; under the no-new-wave boundary, further sampling
would be either prohibited or correlated.

## Exact stopping issue

Prove `O3` without charging the reflected pre-meeting depth linearly.  Concretely, prove the
fixed-constant normalized-range array estimate (6.3), or produce a direct full-state
cancellation/coupling that bypasses the range-triple detour.  No existing file proves this.
