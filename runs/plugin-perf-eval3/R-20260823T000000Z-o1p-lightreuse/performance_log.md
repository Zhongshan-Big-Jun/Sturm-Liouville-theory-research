# Performance Log

Variant: LIGHT REUSE-GATE (revised protocol, round 3)

## Protocol compliance

- Lightweight pre-scan: read research_map.md, tools/README.md relevant
  entries, LEMMA_INDEX.md, and compact summaries/headings of prior runs.
- No per-route REUSE/REUSE_MISS lines were added (protocol did not require
  them).
- Minimum artifact checklist: all required files written (see final_report).
- Post-run reuse summary: reuse_summary.md written.

## Effort observations

- Most context went into the new weighted-shift family derivation and the
  realizability lemma, not into re-reading prior proofs.
- The run/free-base machinery was reused as a black box; this worked because
  it is pure moment algebra.
- The light pre-scan was sufficient: the missing tool entry run-free-base.md
  did not block progress because the relevant definitions were found in the
  prior candidate proofs via heading/search.

## Tool-call / file-creation summary (non-authoritative)

- Files created in run root: all required artifacts plus audit note.
- No long background jobs used.
