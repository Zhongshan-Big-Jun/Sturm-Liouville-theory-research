# Counterexample Log

| Test | Result | Status |
| --- | --- | --- |
| v_1 = x^4, H_{beta,lambda}, any beta >= 0, lambda in (-1,1) | Non-density; degree-2 singleton run obstruction | STRICT (explicit w) |
| r = 0, beta <= 3/2, lambda != 0 | Density in H_{beta,lambda} | STRICT (B_adm empty / finite runs only) |
| r = 0, beta > 3/2 | Non-density in H_{beta,lambda} | STRICT (top infinite run admissible and no constraints) |
| Generic non-shift banded H | Not decided here | OPEN |

No numerical counterexample is used as a proof.
