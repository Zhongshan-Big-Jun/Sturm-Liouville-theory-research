# Reuse summary

## Reused strict inputs

- Parent near-`R=1` sector-inertia anchor.
- Accepted Blueprint M3 large-R branch anchor.
- Parent half-Green sector formulas and exact n=2 symmetric INF branch
  conventions.

## Reuse result

The endpoint anchors localized any failure to a compact middle first zero. The
workers did not repeat the large-R proof or the near-one expansion. They used
the frozen formulas to derive new semiseparable and Jacobi reductions.

## Reuse risks detected

- The old `J^(-1)` monotonicity route is circular at a hypothetical singular
  point unless a different branch chart is supplied.
- The Blueprint query locator did not honor the configured artifact root, even
  though the real artifact and hash were valid.
