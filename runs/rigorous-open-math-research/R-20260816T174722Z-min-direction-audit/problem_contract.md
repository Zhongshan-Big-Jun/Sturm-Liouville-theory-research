# Problem Contract: Audit of `min_direction_progress.tex`

## Source document

- Path: `C:\Users\HuangZY\Downloads\min_direction_progress.tex`
- Title: Sturm--Liouville 间隙问题最小化方向的反射对称性研究进展
- Author: 合作者 (Blueprint v2.2 数学研究记录整理)
- Date: 2026-08-16
- Length: 1388 lines

## Audit task

Determine whether the mathematical content of the document is correct and
consistent enough to be accepted into this repository as a research progress
document. If accepted, add it under `docs/` with an audit trail.

## Completion criteria

1. A structured audit report exists that:
   - identifies every theorem/claim category (Trusted / Reviewed / Certified / Open);
   - checks internal consistency and trust-label honesty;
   - verifies the algebraic/analytic core present in the document (or records
     exactly which parts could not be checked without external artifacts);
   - records an adversarial audit by an independent sub-agent;
   - gives a clear verdict: ACCEPT / ACCEPT_WITH_CAVEATS / REJECT.
2. If ACCEPT, the `.tex` is copied into `docs/`, README/AGENTS updated, and
   changes committed.

## Constraints / provenance

- The referenced external blueprint root `E:/ai_auto_solve/...` is NOT present
  on this machine; referenced run artifacts cannot be opened here.
- The document itself claims several results as `Trusted` (canonical accepted)
  from an external blueprint. Without those artifacts, the audit can certify
  internal consistency and the algebraic core, but cannot independently certify
  the external acceptance status.
- Numerical/computational checks are evidence only, not proof; they are used to
  detect contradictions and to verify exact identities where possible.
