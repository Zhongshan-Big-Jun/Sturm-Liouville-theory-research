# Round 9 software author

## Scope and methods

- User assigns this session as the R9-F01 software author, not the final independent verifier.
- Read the live repository at `/mnt/f/LaTeX/BVE research` using explicit workdir. Only write inside this directory (`/mnt/f/tools/math-audit-round9-20260923/software-author`). Do not modify repository or plugin files; no commit/push.
- Deliver a replacement candidate for `scripts/_gapn2_second_variation_probe.py`, preserving its positional CLI. Read actual imports and table inputs. Execute relevant scripts with `python3 -B`; use existing numerical packages without environment changes.
- New Python code uses tabs. Preserve failed runs and report numerical errors and finite evidence limits. Update this record with each substantive change.
- Check the unequal-width counterexample, Euclidean/optional weighted projections, zero normal, invalid inputs, endpoint-aligned integration with a separate direct fh check, the four requested P1/P2 cases, and resolution/truncation/FD sensitivity.
- P2b already uses integral coefficients; changes there may share robust validation/integration without relabelling it as the same projection bug. P3 concerns finite fixed-width exploration; density Hessian and interface-path acceleration are distinct.

## Conversation and work log

2026-09-23: User explicitly authorized verification and repair of the audit package in Downloads, restricted this author to R9-F01 and this write directory, assigned other mathematics/documentation to the coordinator, and prohibited independent-audit approval claims. Read repository root rules and located the affected script. Existing `/usr/bin/python3` has NumPy 2.5.2, SciPy 1.18.1, mpmath 1.3.0. Applying diagnosing-bugs for a reproducible negative control and writing-for-agents for this handoff record. No broader prior author context is used as evidence.

2026-09-23: Created and ran `regression_projection.py` on the two original P1/P2 AST statements. Unequal-width constant-density test exits 1, old printed residual 0 but actual derivative -8.665090650287976. Ranked checks: wrong coefficient normal; endpoint clipping in old quadrature; independent FD cancellation/spectral truncation. Original dependencies are Recon/roots_of/eigfun, symmetric_root/jac_fd, eigen_data and their NumPy/SciPy imports; table read in full. P2b integral projection is not the F01 mistake. Root rule against local program execution is overridden only for user-authorized relevant probes; no Blueprint maintenance is involved.

2026-09-23: Wrote the first replacement candidate. Added validated Euclidean/width-weighted integral-normal projection, mp analytic target-mode block integrals plus separate tanh-sinh fh checks, Gauss quadrature split at every actual density/direction endpoint, finite-step mp FD without density clipping, and structured outputs. Preserved positional CLI, source dependencies and seeded sample counts. Historical labels are explicitly retired; P2b keeps its integral-based logic, and P3 reports a fixed-width comparison including the separate half acceleration term. Candidate is not yet validated; follow-up runs and any failures remain recorded.

2026-09-23: First full n2/R4/SUP smoke run completed with finite residuals and explicit FD errors. Removed duplicated pairing computation and stated inherited root-search limitations. Added meaningful numerical/invalid-input regression groups and preserved the smoke log as an early candidate result. Test thresholds for exact analytic identities and quadrature comparisons do not replace measured P1 FD/truncation errors.

2026-09-23: Normal-mode regression groups completed successfully, including 41 illegal-input rejections, exact constant-density integrals/finite-step FD, real endpoint adaptive-vs-Gauss checks, and a pulse narrower than the historical grid. Added `sensitivity.py` to measure quadrature orders 16/32/64/128, cutoffs 31/61/121/241, FD steps 1e-1 through 1e-6 and 70/90-digit comparison for fixed n2/R4/SUP directions. Added explicit-cwd `run_evidence.py` to record four full positional-CLI reruns, optimized tests, original-source baseline, CLI rejection tests, timings and hashes. No FD error threshold defines a PASS.

2026-09-23: Four full runs and sensitivity completed. At 61 modes, P1/P2 direct fh residual <= 7.51e-15; SUP samples negative and INF samples mixed. The small third INF P1 value has 0.00500932 relative FD disagreement; retained honestly and scheduled a targeted 241-mode follow-up. CLI compatibility check showed the original accepts R=1; corrected the candidate's overrestrictive R>1 guard to R>=1, retaining the constant-density zero-jump boundary. Saved every prior code/log/result in `pre_R1_compatibility/` before this change. Final reruns add the R=1 CLI case and both normal/optimized tests. The extra follow-up is prompted by measured INF error, not broader repository scope.

2026-09-23: Final candidate SHA-256 `53c5ccd85c28f1a5a1e2e37a4eed4e8dc450025eaf313aae15df028eb1f9d31e` completed 14 recorded commands with expected exits, including the four requested full cases and R=1. Normal/-O each completed 6 regression groups and 41 invalid-input cases. Maximum direct P1/P2 fh residual across four cases is 7.500462406111531e-15. At n2/R4/SUP the maximum relative difference vs MP FD decreased from 1.536054168e-5 (61 modes) to 2.315857015e-7 (241 modes). The small INF sample decreased from 0.005009319859 to 0.00004779746684, with finite errors retained. `assemble_handoff.py` checked candidate/log/result identities and tab INDENT tokens, wrote `REPORT.md`, `summary.json`, `replacement.patch`, `source_preservation.json`, and `syntax_checks.json`. Actual source script/dependency/table/audit hashes stayed unchanged; repository root AGENTS.md changed outside this author's write scope during concurrent work and is explicitly recorded. All conclusions remain software-author finite evidence; mathematical/document changes and final independent review belong to the coordinator.
