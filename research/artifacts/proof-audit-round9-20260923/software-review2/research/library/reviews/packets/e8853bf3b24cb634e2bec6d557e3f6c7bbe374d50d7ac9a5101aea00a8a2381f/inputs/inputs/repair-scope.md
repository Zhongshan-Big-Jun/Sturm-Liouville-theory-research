# Candidate repair and independent execution scope

The first independent review found that positive bump half-widths 1e-20 and 1e-16 could collapse or lose substantial mass in double coordinates. This candidate adds checked_bump_breaks: at least 2**20 coordinate spacings per half-width, radius-error rejection, and checks of the actual quadrature nodes on every interval. The check occurs before expensive probes and again on actual P3 centers. This is a conservative numerical resolution policy, not a certified quadrature error bound. All other calculations are retained; four default author replays have exactly unchanged numeric payloads and logs, apart from file-path/source-hash metadata.

The candidate has not yet replaced the live source: the parent must first receive the historical rejection, then integrate only independently approved exact bytes. Review the candidate itself, not any live project source.

Copy inputs/candidate into a NEW private output directory /mnt/f/tools/math-audit-round9-20260923/software-reviewer2. Its script and three sibling imports/table are complete. Read scripts before executing. Run test_f1.py normally and with -O in that private copy; it writes results next to itself. test_candidate.py is the original regression harness copied beside the NEW candidate, so it tests the candidate, not the rejected copy in the historical zip. Run it normally/-O with --output to fresh files. sensitivity.py also writes beside its copy. Use current CLI defaults for four named cases, plus meaningful R=1 and width-weighted checks. The author run_defaults.py is a provenance record with an external old-output comparison path; do not use it to read outside this packet. Instead compare new outputs with the supplied candidate JSON records, excluding only source_sha256 path/hash metadata.

The first-review zip preserves its frozen inputs and real execution, including original historical source and the first candidate, plus independent checks and the F1 reproducer. Prior verdicts are context, not a substitute for the new review. Do not alter any source project, installed plugin, global configuration or original archive. Save real argv/exit/stdout/stderr, script identities and your final numerical records in the private output directory and identify that directory in your final JSON so the coordinator can archive it.
