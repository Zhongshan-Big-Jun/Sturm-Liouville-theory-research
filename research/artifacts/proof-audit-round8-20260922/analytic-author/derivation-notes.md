# Author derivation checkpoint

For k=sqrt(lambda), eps=R^(-1/2), w=u/eps, ell=1/2-u, define A(theta)=arg(cos(theta)+i eps sin(theta)) continuously from A(0)=0. Then Phi(k)=ell*k+A(w*k). The first DN and DD half-interval roots are Phi=pi/2 and Phi=pi. The first full Dirichlet root is the DN root and the second is the DD root, by positivity/one-node reflection and Sturm oscillation. Phi prime <=ell+w/eps. At the first root theta<pi/2, A(theta)<=theta; hence k1>=pi/(2(w+ell)). Therefore G>=pi^2/[2 eps(w+ell)(w+eps ell)]. For R>=1500 and w<=2 this exceeds 15*pi^2/4, hence both 25 and 3*pi^2. This is an analytic all-region estimate, no grid or unrestricted second-phase branch.

For w>=2, pole-free root identity gives pi/2<theta2<pi. With c=eps*ell/u<=1/4-eps, delta2<=2eps/(pi*c), so z2=c*theta2<=pi/8. Replace the old remainder constant by C=7/20 using sin z-z cos z=int_0^z t sin t dt and sin z>=z-z^3/6. The original deficit identity can be bounded with def1>4 alpha and def2<3 alpha, alpha=eps^2*ell/u, provided B(t)<=8. The latter follows from B<2(t sin t)^2, t sin t<=2 for t<=2, and the tangent-line bound for the concave q(t)=t sin t on [2,pi), using exact Taylor bounds sin2<91/100 and cos2<-2/5. This yields G>Dbar+ell/(R*u^3) for w>=2.

T1 can then avoid all T3 numbers: T2 gives M=Dbar(u*)<3*pi^2, whereas the sliver bound is >15*pi^2/4. Fixed-u convergence follows from pole-free analytic root equations. Near-minimizers with scaled error tending to zero cannot remain in the sliver, and Dbar sublevel compactness plus uniqueness gives u_R->u*. Full derivation and author checks are in progress.
