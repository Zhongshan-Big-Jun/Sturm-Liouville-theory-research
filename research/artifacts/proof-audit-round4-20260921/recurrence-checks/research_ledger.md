# Author ledger

2026-09-21. Read root AGENTS.md, the rigorous-open-math-research skill, full current TeX, fourth-round audit and constructive repairs, the 2026-08-22 root-1 candidate and original audit, and the even c=1 anchor. Recorded baseline source hashes and git state. No relevant mathematical memory entry was found.

The attachments' factorial scaling checks identically for both parities in symbolic c and j. The positive tail construction supports a full three-parameter solution and an explicit raw terminal normalization, avoiding any appeal to unproved Poincare convergence.

The original parameter table misses not only the audited even E+ sign and odd E- sign, but the full family of monic quadratic representations of the cancelled tau=-1 tail (j+sigma)/(j-1). This family can have d nonzero. The normalized E0=1 family cannot contain this tail, because backward uniqueness forces A=0.

Finite-pole mechanism: rational adjacent quotient with nonzero second-difference coefficient would give v=C a R and R(j)-2D(j)R(j-1)+D(j)D(j-1)R(j-2)=1. A leftmost pole cannot cancel; a polynomial cannot satisfy the degree balance. This handles root-0 as well as root-1 and imposes no degree bound.

Box counterexample to check exactly: c=1, even, z0=1,z1=2,z2=5/2, giving rho1=2, rho2=5/4, rho3=7/6-1/480. Its factorial-scaled second-difference coefficient is C=-4; B=3+4(Phi1-Phi0)>2, hence it is a root-1 solution with a nonrational ratio. The asserted degenerate-configuration exclusion therefore fails.

Full author check run completed with exit 0: 2804 exact/symbolic, 32 numerical and 1407 initial static assertions. Final explicit odd E+ and provenance-only source additions were followed by a static-only rerun (1413 assertions), not another mathematical sweep. git diff --check returned 0. No PDF or TeX compilation.

Coordinator steering received: its active d4 rewrite and behavior tests passed; fresh software review was underway. These results are logged as coordinator-supplied and not self-verified. Read the draft card only; its six d=0 rows omit general monic quadratic representations of the A=0 cancelled tail. Sent the exact family and an example in commentary for coordinator alignment; did not edit the card.

Final author-state reconstruction from source, contract, obligation graph, exact outputs and counterexamples finds no known remaining author proof obligation within the stated repair scope. This is author self-check only. Independent mathematical review remains pending and is explicitly required before release. Historical high-order remainder assertions and general coefficient/box/source-term claims are not restored.
