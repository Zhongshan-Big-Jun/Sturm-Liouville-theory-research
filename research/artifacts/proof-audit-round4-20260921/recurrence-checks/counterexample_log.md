# Exact negative controls and omitted boundaries

1. R4-F03 even E+ raw parameters (-1/4,-3/8,-3/4,0) reduce to 1+1/(2j). The old absolute-value parameter formula gives the wrong b.
2. Beyond the attachment's local repair, even (a,b,gamma,d)=(3/2,-1,1,-2) reduces to (j-1/2)/(j-1). It is an A=0 eventual tail. General family is (sigma+t,sigma*t,t-1,-t); d need not vanish.
3. R4-F04 E_j=(1-j/3)H_j has E0=1 but E3=0. A nonzero sequence is not a termwise nonzero reference.
4. The old reduction proof's displayed intermediate expression evaluates to 69/224 at even c=1,j=3,E=E+,z=E-. The corrected intermediate expression vanishes exactly.
5. Even c=1, (z0,z1,z2)=(1,2,5/2) gives delta1=1/2,delta2=0,delta3=-1/480. Its v-branch has C=-4 and B>2, so it is genuinely root-1. Universal degenerate-configuration exclusion is false.
6. Beta=-2m yields an eventually zero product with E0=1, contradicting invertible backward recurrence. Cleared ratio identities must not treat zero factors as admissible division.
7. Shared factors with a positive integer root create a raw 0/0 expression even if the reduced rational function has a removable singularity. Neither that extension nor an eventual tail can overwrite a sequence's actual zero term.
