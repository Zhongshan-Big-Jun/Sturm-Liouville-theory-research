This is a selected copy of the original author evidence and a separate coordinator replay. Original absolute paths and manifests are preserved as execution records; intermediate compiled objects and installed Mathlib binaries are not republished. The original complete author freeze remains at the machine path recorded in FREEZE.json.

For a new run, use Lean 4.31.0 and the exact lake-manifest revisions under pins/. Compile the current lean-proof/SL/AuditRound3.lean from the lean-proof working directory into a fresh output directory. Put that directory first in LEAN_PATH. Adapt the output directory of InspectAuditRound3.lean before running it: it writes declarations/types/axiom evidence. The recorded runner and replay helper show the WSL/native Windows path conversion; never replay them with their old output paths, which are frozen.

The coordinator replay produced byte-identical actual declarations, definition closure and compiled root object. The two controls under controls/ must fail with unsolved False. Reported transitive axioms are limited to propext, Classical.choice and Quot.sound. This does not certify analytic integrals, convergence, completeness, the full old Lean project or independently rebuild dependency binaries.
