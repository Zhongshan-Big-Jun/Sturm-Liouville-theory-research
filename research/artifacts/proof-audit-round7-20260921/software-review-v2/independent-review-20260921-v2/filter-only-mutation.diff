--- frozen/regression/provenance.py
+++ MUTANT/regression/provenance.py
@@ -54,7 +54,7 @@
     # Deliberately no Base/path allowlist filter at observation time.
     return {name: str(Path(module.__file__).resolve())
             for name, module in list(sys.modules.items())
-            if getattr(module, '__file__', None)}
+            if getattr(module, '__file__', None) and Path(module.__file__).resolve().is_relative_to(Path(__file__).resolve().parents[1])}
 
 
 def expected_bindings(tree, kind):
