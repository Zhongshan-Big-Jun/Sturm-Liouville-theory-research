# Round7 evidence entry

Read [the scoped repair report](../../../reports/proof-audit-round7-20260921/REPORT.md) first. Current reviews are under [reviews](reviews/summary.json); current card releases and actual retrieval are [library-final.json](library-final.json) and [default-query.json](default-query.json). Software v2 and numerical v2 reports describe fresh executions; independent-execution-v2 is the fresh Lean replay. The unversioned software-review and independent-execution folders preserve earlier rejected/incomplete attempts.

Each agent archive has archive-manifest.json and ARCHIVE.md with original paths, hashes, stored paths and reversible gzip/object mappings. Original execution paths inside raw records are intentionally unchanged. Compiled objects are archival .bin files; derived indexes are labeled. The private F:/tools AGENTS context is excluded where marked, with its identity retained; it is not an executed dependency. Coordinator records are actual captured tool envelopes, not cryptographic service attestations.

The current mathematical source is bound in the final review packet. The report explains the local Lean scope and the legacy precise backend defect. No canonical integration, old proof replacement, full Lake build or blanket historical numerical certification is implied.
