# Independent execution scope

Review only this packet. Do not read project history, memory, author conversation or prior verdicts. Standard installed Python scientific packages are permitted. Copy inputs/scripts into a NEW private directory under /mnt/f/tools/math-audit-round11-20260926/software-reviewer/. Copy the remaining inputs beside it. All executions use that private root as cwd; no main-project writes. Check every loaded _gapn2*, _sl_prufer and reflection_seeds module, without filtering unexpected modules before checking, resolves to the private copied source. Record all module origins and hashes.

Read code before execution. The author check script is portable: python -B check_jacobian.py PRIVATE_ROOT PRIVATE_RESULTS BEFORE_JACOBIAN_FILE. Execute normally and with -O; this is regression evidence, so add your own adversarial cases. Independently check exact R=1 residual derivatives, the narrow clipping example, arbitrary coupled derivatives, n=1 and n=3 determinant signs, non-symmetric/commuting matrices, nonfinite or malformed callbacks, unchanged input arrays, tiny or infeasible/unresolvable steps, converter-induced drift and observable endpoint motion. Do not claim geometry tolerances certify derivatives. Reproduce the old AST functions as limited evidence; do not run their old main.

Personally execute the frozen actual Jacobian CLI 1,4 2 both, the O3 CLI 2,3 4 both 80, and both default second-variation CLI cases 2 4 sup/inf with --output paths in your private results. Source files are siblings under scripts. Distinguish residual J's cross blocks from Hessian/K's commuting blocks, physical reflection from coordinate reversal, odd-n determinant sign, R=1 zero density jumps, and finite eigensum truncation from certified results. Examine the P3 saved finite-difference geometry, matrix multiplication and stationary-point limitations. No full historical scan or global G1 sign is requested or justified.

Keep actual argv/cwd, timestamps, return codes, stdout/stderr, source hashes before/after, module identity checks, own test sources and outputs. Name your external evidence directory in the final JSON. Findings need concrete reproducers and an exact affected contract. Approval must follow your own execution, not author labels.
