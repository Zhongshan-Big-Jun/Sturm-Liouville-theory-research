# Required independent formal verification

Personally compare every declaration/definition and the full literal root type against the human contract, original input statements and separately supplied blind readback. Inspect the actual paired ordering (the second half is mirrored-pair order), real scalars, n=0, zero vector, conditional JP=-PJ, arbitrary real L versus natural L>=4, explicit boundary entries and the missing physical/complex/Sobolev bridges. Numerical software correctness is not a consequence of this packet.

Create a NEW private execution workspace under /mnt/f/tools/math-audit-round11-20260926/formal-reviewer/. Copy only frozen inputs; never write author/project files. You may adapt run.py to your own base paths and reviewer role. Use the exact Lean 4.31.0 runtime at /mnt/f/DevCache/elan/toolchains/leanprover--lean4---v4.31.0 and the recorded read-only package artifacts under /mnt/f/LaTeX/BVE research/lean-proof/.lake/packages. Inspect only recorded dependency paths and installed verifier. No full Lake build, dependency download, unrelated research source or past verdict is needed.

Actually compile AuditRound11.lean in that workspace, match its literal root against contract.json, and re-export all 37 actual declarations/definition bodies and transitive axioms. Compare the author export, making only explicit path adaptations for ExportDeclarations.lean. Personally invoke the unchanged installed lean-verify 2.0.1 verify_lean_project.py with --direct, --strict-exit, explicit --lean/--lake and a fresh output directory. Inspect its actual loaded module and local object resolution, runtime/source/contract hashes, root closure and foundational axioms; do not merely reuse author PASS. Main source has 14 authored theorems and 12 authored definitions/abbreviations, while export includes generated declarations.

Run the positive mathematical controls and all four deliberate mathematical negatives; verify that rejection is due to the intended false determinant/block/boundary/reflection assertion rather than import/syntax/timeouts. Check the corresponding positive witnesses yourself; add a direct complementary control if the supplied witness does not address a negative. Separately exercise accepted closed target, hidden axiom contamination, wrong expected type and missing declaration through the original verifier, or justify any genuinely unavailable check. The intentional axiom is in a separate control project, not imported by the main source.

Record actual argv/cwd/environment, source hashes, all exits/stdout/stderr and failed attempts. Save a complete SHA-bound execution manifest and compact verification record in your private directory, and report exact paths in the required final JSON. The root manifest supplied here is losslessly gzip-compressed; structured inspection avoids dumping its huge type. Author machine semantic:not_reviewed correctly indicates author-only work; your verdict remains a local algebra/interface semantic review, never full ODE, critical-trace/cofinite proof or canonical acceptance.
