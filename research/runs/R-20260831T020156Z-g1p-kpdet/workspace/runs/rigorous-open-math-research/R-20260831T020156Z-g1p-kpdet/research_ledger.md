# Research ledger

## 2026-08-31, step 1. Scope and provenance

- Loaded the active workflow, program-management, and rigorous-research skills.
- Used the installed Blueprint gateway. Parent validation passed with 9 nodes, 10 edges, and 3 linked inventory rows.
- Recomputed the parent commit and seven source hashes.
- Recorded the B0 verdict `PARTIALLY_OPEN`; no global novelty claim is made.

## 2026-08-31, step 2. Scoped gate

- Created a self-contained logical project under `research/runs/`.
- Scoped pipeline result: 0 problems and 2 expected active-run warnings.
- Whole-project legacy migration debt remains visible and is not represented as passing.

## 2026-08-31, step 3. Direct closure-first attack

- Restated KP-DET with the audited normalized matrix convention.
- Reused only hash-bound strict inputs from the previous independent PASS.
- Derived the exact final-layer quotient derivative.
- Proved `gamma_2>b_0` for every finite-interior branch point.
- Concluded `(Kp_odd)22<0` globally on the branch.
- Reduced KP-DET to the single scalar Schur margin `S_KP<0`.
- Promoted no numerical evidence to proof.

## Observable work data

- Research workers dispatched before the closure gate: 0.
- Web search calls: 3 bounded primary-source calls.
- Project-local Python tools run or copied: 0.
- New strict claims before independent audit: 1.
- Current upstream status: `RUNNING`.

## 2026-08-31, step 6. Sequence-04 PHI exact route

- Verified checkpoint sequence 03 as `READY` and advanced immutable mutable
  bindings to sequence 04.
- Coordinator direct work proved the exact safe equivalence
  `Phi<0 iff Xi>0`, with no division by middle-layer trigonometric factors.
- Closure gate changed from `OPEN_EXACT_GAP` to a one-worker `ESCALATE`.
- W3 returned `PARTIAL`, artifact SHA-256
  `6c1bbbd871c9aef7c8c316d47d0b61c3ab34bfb806eb5ee715ed915df6103ee3`.
- W3 converted the exact mass equation to the spectral radial-slope identity
  `(M-slope)`, proved `K<0`, and derived
  `Xi=X^2G-rKDtheta`.
- The sufficient route `G>=0` remains unproved. More generally, the exact
  mass-slope to `Xi>0` sign bridge remains open.
- The worker's finite continuation probe is labeled `EVIDENCE` only. It is
  not used in any strict claim.
- Research workers dispatched in sequence 04: 1. Worker restarts: 0.
  Duplicate dispatches: 0. Transcript replays: 0.

## 2026-08-31, step 7. Sequence-05 independent W3 audit

- Checkpoint sequence 04 sealed and verified `READY`, then advanced to
  sequence 05 before the audit call.
- A fresh reviewer independently re-derived the coordinator and W3 package.
- Verdict: `PASS`. Critical errors: 0. Gaps in claimed strict identities: 0.
- Audit JSON SHA-256:
  `3bace4993b5a14c55950043322dd410e65f7f0135df5e03c95dde18a5ad6b3dd`.
- Audit Markdown SHA-256:
  `d8398556b5f19926b98448f6e87bdb53ab9dc18efc520e223f8801bef460cb6b`.
- Residual risk: no mass-slope implication to `G>=0` or a weaker sufficient
  lower bound has been proved. `PHI-SIGN` and KP-DET remain open.
- Audit-model responses in sequence 05: 1. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.

## 2026-09-01, step 8. Sequence-06 mass-to-G two-arm wave

- Checkpoint sequence 05 sealed and verified `READY`, then advanced to
  sequence 06 before dispatch.
- Dispatched exactly two isolated workers: W4 exact prover and W5 exact
  falsifier. Each used one response and no subagents.
- W4 returned `PARTIAL`, SHA-256
  `d55114570d516c69e446f2c228a76fb8827335e596df6c62e3d355a5232f9ffa`.
- W4 localized the mass equation to a candidate strict mixed-sign layer
  balance and isolated the open sign-coherence implication `(SC)`.
- W5 returned `PARTIAL`, SHA-256
  `03a06fbe30ae7acea06a7da21d694f3d07bb3140458c93ce78b16b911fefb9e9`.
- W5 produced a candidate exact spectral-band-modal, mass-defective point
  with `G<0` and `Xi<0`; it is not a complete-system counterexample and is
  used only to eliminate mass-free proof shortcuts.
- Both returns are unreviewed until the next fresh independent audit.
- `PHI-SIGN` and KP-DET remain open.
- Research-model responses in sequence 06: 2. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.

## 2026-09-01, step 9. Sequence-07 joint W4/W5 audit

- Checkpoint sequence 06 sealed and verified `READY`, then advanced to
  sequence 07 before the audit response.
- A fresh reviewer independently rederived W4 and checked W5 as a first-time
  submission.
- Verdict: `REPAIRABLE_GAP`. Critical errors: 0. Repairable gaps: 1.
- Audit JSON SHA-256:
  `7e56fc988a361efa5aeec7d232fb43b03b7889dacfc8ebc3d4afd6a02231c175`.
- Audit Markdown SHA-256:
  `97964c01b5ee868c784edd687819bbda5e3604155d197fb036cf70e25cc740d5`.
- Accepted strict partials: all W4 layer-coefficient formulas, the strict
  mixed-sign theorem, and the W5 exact mass-defective witness with strict
  `G<0`, `Xi<0`, and `Delta_M>0` certificates.
- Downgraded claim: the W5 restricted near-one theorem, because uniform
  convergence over moving switch positions was asserted without the needed
  compactness and endpoint-separation proof.
- `PHI-SIGN` and KP-DET remain open.
- Audit-model responses in sequence 07: 1. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.

## 2026-09-01, step 10. Sequence-08 W5 near-one repair

- Checkpoint sequence 07 sealed and verified `READY`, then advanced to
  sequence 08 before the repair response.
- The original W5 author used one bounded response and no subagents.
- Repair status: `REPAIRED`, artifact SHA-256
  `8defee6c05565313b5d9f2e4365d102349c32e8cf9ef04bde6f288ace6c30314`.
- The repair supplies a candidate uniform compactness chain for moving
  switches, including a switch-independent `beta` bound, min-max spectral
  convergence, exact norm limits, mass-limit endpoint separation, uniform
  positivity of `U`, and divergence of `Dtheta U`.
- Claimed scope: fixed `eta>0`, `eta<=alpha<=pi-eta`, and `m->1+`.
- The simultaneous face `alpha->0`, global `G>=0`, `PHI-SIGN`, and KP-DET
  remain open.
- Repair-model responses in sequence 08: 1. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.

## 2026-09-01, step 11. Sequence-09 near-one re-audit

- Checkpoint sequence 08 sealed and verified `READY`, then advanced to
  sequence 09 before the re-audit response.
- The prior independent reviewer re-audited only the immutable repair.
- Verdict: `PASS`. Critical errors: 0. Gaps: 0.
- Re-audit JSON SHA-256:
  `3f88a6ed8cf6da7f7adc41a195776fbfa9f00c8cec97153156a98f773a0c573d`.
- Re-audit Markdown SHA-256:
  `46358dcdcc53a772190593d1dbd5b81bda5711bb4b46420a9fc817b807c45e52`.
- Accepted theorem: for every fixed `eta>0`, complete moving-switch tuples
  with `eta<=alpha<=pi-eta` satisfy `G>0` uniformly for `m->1+`.
- The remaining near-one escape face is `alpha->0`. Arbitrary finite `R`,
  global `G>=0`, `PHI-SIGN`, and KP-DET remain open.
- Re-audit-model responses in sequence 09: 1. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.

## 2026-09-01, step 12. Sequence-10 alpha-collision wave

- Checkpoint sequence 09 sealed and verified `READY`, then advanced to
  sequence 10 before dispatch.
- W6 was rejected by the service at the usage boundary before producing any
  mathematics or artifact. It is recorded as `NO_RETURN`, not a result.
- W7 returned `PARTIAL`, artifact SHA-256
  `191b0a1cd621b8f8451647a5273a2f79efd0d57b71e3a7ba570e8644cae6e044`.
- W7 gives a candidate exact contradiction excluding every complete
  admissible sequence with `m->1+` and `alpha->0`.
- The proposed mechanism combines uniform spectral limits, removable norm
  singularities, the exact mass limit, and the exact band ratio.
- Valid research-model responses in sequence 10: 1. Service-rejected turns:
  1. Worker restarts: 0. Duplicate dispatches: 0. Transcript replays: 0.
- The W7 theorem is unreviewed. `PHI-SIGN` and KP-DET remain open.

## 2026-09-01, step 13. Sequence-11 W7 independent audit

- Checkpoint sequence 10 sealed and verified `READY`, then advanced to
  sequence 11 before the audit response.
- A fresh independent reviewer rederived W7 as a first-time submission.
- Verdict: `PASS`. Critical errors: 0. Gaps: 0.
- Audit JSON SHA-256:
  `e34da0e0bbf78f569706a095c19d3eb524b0be5f0c5e7fe9361de328c3bb9fdb`.
- Audit Markdown SHA-256:
  `42fb8c2ba28624aa3a90074f55a686bc80554122af8e6ceeffd3e2200a6bb103`.
- Accepted theorem: no complete admissible sequence has `m->1+` and
  `alpha->0`; equivalently, a uniform near-one alpha-zero wedge is empty.
- Boundary correction: the fixed-eta theorem also excludes `alpha->pi` from
  its hypotheses. That distinct face remains to be analyzed before the
  mechanism-level near-one coverage is complete.
- Audit-model responses in sequence 11: 1. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.
- Arbitrary finite `R`, `PHI-SIGN`, and KP-DET remain open.

## 2026-09-01, step 14. Sequence-12 alpha-pi endpoint wave

- Checkpoint sequence 11 sealed and verified `READY`, then advanced to
  sequence 12 before dispatch.
- W8 returned `PROVED`, SHA-256
  `b0f66b3090280f946d2ec4d49df54eed942ae56913aa77d286e1ce8e028881cb`.
- W9 returned `REFUTED` for endpoint-family existence, SHA-256
  `ece86c1ff05afa17a3fdb6f9bab94e31b69cbdf38190e2a6c1d1b77a10e5b514`.
- Both routes independently derive `theta->pi/2`, `beta->0`, finite norm
  limits, and an exact mass contradiction `0=pi/6`.
- The common candidate theorem is a uniform empty wedge near
  `(m,alpha)=(1,pi)`.
- Both outputs are unreviewed. Arbitrary finite `R`, `PHI-SIGN`, and KP-DET
  remain open.
- Research-model responses in sequence 12: 2. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.

## 2026-09-01, step 15. Sequence-13 W8/W9 joint audit

- Checkpoint sequence 12 sealed and verified `READY`, then advanced to
  sequence 13 before the joint audit response.
- Verdict: `PASS`. Critical errors: 0. Gaps: 0.
- Audit JSON SHA-256:
  `6cf59c106103464c8704ab4aafea8c1833d9437199472b4411727ccc956fc514`.
- Audit Markdown SHA-256:
  `ddaa901bc72afbbddc467791ab82b8265cd7c30dfe33425dd57f62ab11aaa574`.
- Accepted theorem: a uniform alpha-pi empty wedge with exact mass residual
  limit `-pi/6`.
- Accepted assembly: the alpha-zero wedge, alpha-pi wedge, and one fixed strip
  yield a common near-one epsilon on which every complete tuple has `G>0`
  and `Phi<0`.
- Audit-model responses in sequence 13: 1. Worker restarts: 0. Duplicate
  dispatches: 0. Transcript replays: 0.
- Arbitrary finite `R`, global `PHI-SIGN`, and KP-DET remain open.

## 2026-09-01, step 16. Sequence-14 global sign-coherence wave

- Checkpoint sequence 13 sealed and verified `READY`, then advanced to
  sequence 14 before dispatch.
- W10 returned `PARTIAL`, artifact SHA-256
  `8f5c381223b476fcf2b2d6af7f1a51e90086c3146b45a48bdd8362ad126f11aa`.
- W10 proposes an exact phase lock, the factorization
  `G=X(M Dtheta/P)(q-E)`, an exact `B`-to-`H` identity, and the
  complete-system exclusion `B<0`.
- W11 returned `PARTIAL`, artifact SHA-256
  `18f2e57dfd18784527dac95d07477e89da935fff4065658f2a847af9137e4ba8`.
- W11 proposes an exact one-parameter negative-`G` spectral-band family with
  positive coefficient chamber and positive mass residual. No complete
  counterexample was found.
- Both outputs are unreviewed. Global `(SC)`, `G>=0`, `PHI-SIGN`, and KP-DET
  remain open.
- Research-model responses: 2. Worker restarts: 0. Duplicate dispatches: 0.
  Transcript replays: 0.

## 2026-09-01, step 17. Sequence-15 W10/W11 joint audit

- Checkpoint sequence 14 sealed and verified `READY`, then advanced to
  sequence 15 before the audit response.
- Verdict: `PASS`. Critical errors: 0. Gaps: 0.
- Audit JSON SHA-256:
  `11b3b68b8aa9b1dcfd593b1e919169f9057f3daa63ef1dfb6ccb09a46da7e1db`.
- Audit Markdown SHA-256:
  `579cc71e692a26de07b5df2f45127910de637180634aee66d84b0f55f8cd04c5`.
- Accepted W10 strict partials: exact phase lock, factorization
  `G=X(M Dtheta/P)(q-E)`, equality set, differential form, exact `B`-to-`H`
  identity, and complete-system exclusion `B<0`.
- Accepted W11 strict partial: an exact open mass-defective negative-`G`
  family trapped in the positive coefficient chamber with positive mass
  residual, including W5.
- Audit-model responses: 1. Worker restarts: 0. Duplicate dispatches: 0.
  Transcript replays: 0.
- Global `(SC)`, `G>=0`, `Xi>0`, `PHI-SIGN`, and KP-DET remain open.

## 2026-09-01, step 18. Sequence-16 common-beta orientation wave

- Checkpoint sequence 15 sealed and verified `READY`, then advanced to
  sequence 16 before dispatch.
- W12 returned `PARTIAL`, artifact SHA-256
  `6ecc0ae44f6841414a8a8be8077ed919f1d66d285dc66abbdc79f85660c44d6d`.
- W12 proposes a branch-safe unsquared common-`beta` identity, a positive
  square-root lock, unique acute reconstruction, and strict KP-DET closure
  for `c alpha<=pi/2`, including all complete tuples with `0<c<=1/2`.
- W13 returned `EVIDENCE`, artifact SHA-256
  `61ff0e77fac55e0496d08720b0f06315f9617a8cb38d347e23fbbf43445d6135`.
- W13 found no mixed-chamber or numerically mass-balanced `q>E` tuple in its
  bounded scan. This is not an exhaustive or interval-certified result.
- Both outputs await fresh audit before strict reuse.
- Research-model responses: 2. Worker restarts: 0. Duplicate dispatches: 0.
  Transcript replays: 0.
- Arbitrary finite-`c` `PHI-SIGN` and global KP-DET remain open.

## 2026-09-01, step 19. Sequence-17 W12/W13 joint audit

- Checkpoint sequence 16 sealed and verified `READY`, then advanced to
  sequence 17 before the audit response.
- Verdict: `PASS`. Critical errors: 0. Gaps: 0.
- Audit JSON SHA-256:
  `bb1207baf181f37459345ed3cff4deb560b5c0acc18fdc3952b8410ffb6bd820`.
- Audit Markdown SHA-256:
  `22b69f39e6d5cb1f12cf6b112b9d0a48e6fe40e36c5bf78a03c9a109272cff99`.
- Accepted strict partials: branch-safe common-`beta` identity, positive
  square-root lock, coefficient dictionary, unique acute reconstruction, and
  strict KP-DET closure for `c alpha<=pi/2`.
- Accepted corollary: every complete tuple with `0<c<=1/2` satisfies KP-DET.
- W13 remains `EVIDENCE` only.
- Audit-model responses: 1. Worker restarts: 0. Duplicate dispatches: 0.
  Transcript replays: 0.
- The acute scalar threshold, arbitrary finite-`c` `PHI-SIGN`, and global
  KP-DET remain open.

## 2026-09-01, step 20. Sequence-18 acute-threshold wave

- Checkpoint sequence 17 sealed and verified `READY`, then advanced to
  sequence 18 before dispatch.
- W14 returned `PARTIAL`, artifact SHA-256
  `ef7ad48667026a5eb672c8d4bd48718903fd6dcf2102d9f16b8a6883ece948c2`.
- W14 proposes strict constrained compatibility monotonicity, exclusion of
  the acute branch for `c<=2/3`, and an exact one-scalar mass collapse for
  the remaining `c>2/3` branch.
- W15 returned `PARTIAL`, artifact SHA-256
  `c961bcba5931957beb2e2e60baed90e9517d2af0d3015efa8605a86e985160a2`.
- W15 proposes a uniform all-`m` collar classification where the strong
  threshold has positive margin but the normalized mass residual is strictly
  negative. Its bounded searches remain `EVIDENCE` only.
- Both analytic outputs await fresh independent audit.
- Research-model responses: 2. Worker restarts: 0. Duplicate dispatches: 0.
  Transcript replays: 0.
- Arbitrary finite-`c` `PHI-SIGN` and global KP-DET remain open for `c>2/3`.

## 2026-09-01, step 21. Sequence-19 audit no-return

- Checkpoint sequence 18 sealed and verified `READY`, then advanced to
  sequence 19 before dispatch.
- The single fresh audit response was rejected at the service usage boundary
  before producing mathematics or an artifact.
- Outcome: `NO_RETURN`; this is not an audit verdict.
- W14 and W15 remain immutable and `UNREVIEWED`.
- Valid audit responses: 0. Service-rejected turns: 1. Worker restarts: 0.
  Duplicate dispatches: 0. Transcript replays: 0.
- The next authorized model action after quota recovery is exactly one fresh
  joint audit bound to the same hashes.

## 2026-09-01, step 22. Sequence-20 zero-model recovery normalization

- Verified sequence-19 as `READY`, wrote immutable `resume_receipt-19.json`,
  and advanced once without any model, worker, network, or transcript replay.
- The versioned sequence-20 closure gate preserves the historical
  `AUDIT_REQUIRED` note under a non-authoritative label, leaving the canonical
  gate decision `OPEN_EXACT_GAP` unchanged.
- Sequence-20 checkpoint ID:
  `sha256:db12944f10c127db1adca4fe977d1e4bc5063c9fe040750245db9699046b917b`.
- Checkpoint verification: `READY`, 43 bound artifacts.
- Scoped pipeline validation: 0 problems and 2 expected warnings. Scoped PASS
  does not imply a whole-project PASS.
- The only next model action remains
  `AUDIT-W14-W15-ACUTE-RETRY-01`. W14/W15 remain `UNREVIEWED`.
