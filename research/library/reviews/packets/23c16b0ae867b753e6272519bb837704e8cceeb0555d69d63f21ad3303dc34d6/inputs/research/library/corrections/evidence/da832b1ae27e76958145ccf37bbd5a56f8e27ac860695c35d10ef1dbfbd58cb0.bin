# 第七轮证明审计报告

日期：2026-09-21  
仓库：Zhongshan-Big-Jun/Sturm-Liouville-theory-research  
固定提交：`636dac87c269795a520824101f1b2abd2c583c9b`  
该提交的时间：2026-09-21 10:39:28 UTC

## 1. 结论与范围

第六轮新修订的两条主要证明链——完整原稀疏族的 `0<=s<7/2` 窗口及投影/有限矩修复——在本轮所核对的关键步骤中没有发现新实质缺口。不能继续把已经修好的投影推论、空洞有限矩条件或旧分数阶范围当作当前错误报告。

扩查转向目前研究导航仍直接引用的 `docs/SL_gap_nge2_symmetry_local_proof.tex` 和 `tools/gap-band-extremals.md`，确认四组问题：

| 编号 | 对象 | 性质 | 结论处置 |
|---|---|---|---|
| R7-F01 | 常密度 Wronskian 与端点展开 | 明确错误等式 | 替换公式；严格负性和常密度根结构可以保留 |
| R7-F02 | `lem:away` 的一致端点估计 | 可修复的实质证明缺口 | 用全区间 Volterra 估计替换全局 C3/首块论证 |
| R7-F03 | 一般 n 的 SUP 分支/极值趋势写成 `4pi^2` | 错误的范围推广，原位置是趋势/证据段落 | 将 n=1 特例限回 n=1；一般上确界极限是 `(n+1)^2*pi^2` |
| R7-F04 | 对称移动两接口的谱隙导数 | 丢失因子 2 | 修工具卡导数；驻点方程 `f=0` 不变 |

这不是原局部对称性定理被反例推翻，也不否定 n=1 的 `4pi^2`。本轮附上端点引理的完整替换证明，以及一般指标谱隙上确界极限的独立证明。

没有修改或提交仓库。没有运行仓库原程序、Lean 或历史全套区间证书。没有完整审查 M3/KP、所有非对角子类或全部历史研究包；这里不作全仓库通过声明。

## 2. 第六轮修订复核

### 2.1 完整分数阶窗口

当前 `docs/SL_fractional_left_definite.tex` 已分别证明：

- 两个仿射低模态和全部正频谱系的归一化；
- 任意固定多项式的谱系数 `O(k^-2)`，第一层边界相容多项式的谱系数 `O(k^-4)`；
- 对全部 m>=2，两支下一层边界首项分别为 `4m(4m-5)` 与 `4m(4m-3)`，非零；
- 由非零首项得到每个非仿射命名成员的精确阈值 `s<7/2`；
- 四迹矩阵的行列式 `15360`，固定右逆和真正平方域的对应；
- 在四阶相容多项式中逼近，再以谱截断向目标范数传输。

本轮重新检查这些接口和局部代数；没有把 H3 稠密性错误地向更强范数外推，也没有把域外命名成员逐个赋予 Hc4 范数。该新主链可保留。

### 2.2 投影与有限矩判据

当前 `docs/SL_projection_moment_repairs.tex` 区分了全部多项式投影、稀疏族投影和实际保留族；修复后的消去子为 `V intersect M^perp`，无条件增广需要 `P_V x^2,P_V x^3`。

低阶有限测试被放在真正的有限维尾部障碍空间，而非整个无限维 V。一般三角尾部的代数补空间和低矩单射论证正确保留了完整次数及非零首项要求。没有发现这些复核环节的新错误。

## 3. R7-F01：常密度公式错误，但负性可以严格保留

源：`docs/SL_gap_nge2_symmetry_local_proof.tex`，`thm:r1` 的证明及同段端点展开。

对 `u_k=sqrt(2) sin(k*pi*x)`，源文写
`W=-2(n+1)pi sin(pi x)`。

正确式为
\[
W=\pi[\sin((2n+1)\pi x)-(2n+1)\sin(\pi x)]
=-4\pi\sin(\pi x)\sum_{j=1}^n\sin^2(j\pi x)<0.
\]
精确反例：`n=2,x=1/2`，正确值 `-4pi`，源式 `-6pi`。

同段的切换函数端点展开必须是
\[
f_1(x)=2\pi^4(n^4-(n+1)^4)x^2+O_n(x^4),
\]
而不是源文的 `2pi^2`。

以上均不改变结论需要的符号。n=2 的两个 t 根 `(11 +/- 2sqrt(10))/36` 正确；对应归一化 Jacobian 行列式精确为
\[
\frac{7030400000}{4782969}\pi^4>0,
\]
与源文约 `1.43180e5` 一致。不应把正确的根式和非退化性一起撤回。

## 4. R7-F02：全局 C3 与首块宽度的证明问题

源：同文件 `lem:away`。

文稿试图对闭单纯形内全部配置获得统一端点邻域，理由包括“特征函数一致 C3”和首块内 Taylor 展开。但有效密度跳点 a 处
\[
[u_k'']_a=-\lambda_k[\rho]_a u_k(a).
\]
当函数值不为零时，此跳量非零，函数一般不是全局 C2，更不是 C3。

此外闭单纯形允许首块宽趋零。首块内的估计不能直接覆盖统一的 `(0,delta)`。原证明又将“第一接口”和“切换函数第一零点”都记为 x1；对于引理量化的任意配置，二者尚未被证明相同。正因为引理用于排除边界趋近，不能预先要求首块有正下界。

处理方式：不否定引理结论，替换证明。

附录 B 对全部可测 `1<=rho<=1+epsilon` 给出统一射击估计
\[
y_k(x)=x-\lambda_k\int_0^x(x-t)\rho(t)y_k(t)dt,
\qquad |y_k(x)/x-1|\le C_nx^2,
\]
并由变分谱夹逼、Volterra 连续依赖和权归一化得到统一端点斜率间隙。于是直接推出两端 `f_rho<0`，不要求最小块宽或高阶光滑性。

这可修复局部定理用于从隐函数定理邻域唯一性推广到整个升序区域 U 唯一性的关键一步。结合 C1 根隔离还可以补足“块中点符号正确”向“整块符号正确”的说明。并未证明全 R 的 G1'/G2。

## 5. R7-F03：一般 n 不能继承 n=1 的 4pi^2

源：同文件 `sec:global` 中“开放条件的现状”注，SUP 分支的大 R 描述；相关早期工具卡和数值报告还应核对 n 的范围。

先令
\[
S_n(R)=\sup_{1\le\rho\le R}(\lambda_{n+1}(\rho)-\lambda_n(\rho)).
\]
常密度 `rho=1` 给出
\[
S_n(R)\ge(2n+1)\pi^2.
\]
所以 n>=2 时，上确界不可能趋于 `4pi^2`。这不影响 n=1 的已知结论。

原段落也以带自洽 SUP 分支的趋势书写。对于从 R=1 出发、对 R 可微且保持带自洽的分支，接口项为零，故
`dD/dR=int_{rho=R}f>0`；这种分支也不能从 `(2n+1)pi^2` 降到 `4pi^2`。
不以此预设全 R 分支无折点或唯一性。

本轮附录 D 另给出一般上确界极限的严格证明：
\[
\lim_{R\to\infty}S_n(R)=(n+1)^2\pi^2.
\]
证明用 n 个等距薄高密度区间：n 维平台试探空间使 `lambda_n->0`，在这些中心取零的余维 n 子空间使 `lambda_(n+1)->(n+1)^2pi^2`。变分上界反向夹逼即可。

准确不等式：令 `h=1/(n+1)`、`delta=min(h/12,R^(-2/3))`，构造权满足
\[
\lambda_n\le\frac6{hR\delta},\qquad
\lambda_{n+1}\ge\left(\frac{h^2}{\pi^2}+\frac{(R-1)\delta^2}{2}\right)^{-1}.
\]
本结论同时覆盖可测盒类及有限分块常值类；不确定有限 R 的最优接口，也不解决 INF 分支。

## 6. R7-F04：工具卡遗漏镜像因子

源：`tools/gap-band-extremals.md`，“Feynman–Hellmann 对称加倍”段。

同一个独立参数令左接口增加 epsilon、右镜像接口减少 epsilon 时，应有
\[
\frac{dD_n}{d\epsilon}=2(\rho_L-\rho_R)f(x_j).
\]
源文在单特征值公式中写了 2，相减得到谱隙导数时却丢掉 2。

独立交叉验证选取非驻点的三段对称权 `[1,4,1]`，两接口为 `1/4,3/4`，n=1。
精确分块积分归一化及高精度射击给出
`dD/da=53.8809721602405715236952429875`，旧式为其一半。
驻点处两式都为零，所以仅检验自洽残差无法识别这个错误。

驻点条件 `f(x_j)=0` 不变；不据此撤回由该零点方程定义的配置。需要核对的是导数、梯度或 Hessian 若复用这一公式，其参数和系数是否统一。本轮没有检查全部作者程序是否真的使用旧式。

## 7. 执行记录与可信度边界

- 固定提交下，通过 GitHub 连接器读取证明内容与提交元数据；本轮没有克隆仓库。容器直接网络读取不可用，未声称获得或哈希核对全部原始源码字节。
- 独立脚本 `checks.py` 在正常模式和 `python -O` 下均退出 0。
- 共 18 个具名组：14 个代数/符号或有限精确组，4 个高精度或解析上下界的有限取值组。
- “确认”包括确认错误公式被反例否定，不是 18 个仓库定理获得机器证明。
- 无限维端点估计与一般 n 极限由 `spectral_repairs.md` 的完整解析证明承担，数值扫描不承担这些结论。
- 没有运行原作者测试套件、Lean、PDF 编译或 M3/KP。没有另一个独立审稿人对本轮补证的验收。

## 8. 最小修改顺序

先改局部对称性文稿中的 W 与端点系数，再以全密度 Volterra 证明替换 `lem:away`；随后修正一般 n 的 SUP 极限范围，并同步工具卡的镜像因子。保留第六轮的完整分数阶窗口和投影修复。

若修订保留旧冻结材料，应新增版本绑定的勘误和当前入口，不事后改写历史审计为“早已发现”。

## 9. 可定位来源

全部链接固定到本轮提交，文件内请按命题标签定位，不用文件名相似性替代依赖核对。

- [完整分数阶证明](https://github.com/Zhongshan-Big-Jun/Sturm-Liouville-theory-research/blob/636dac87c269795a520824101f1b2abd2c583c9b/docs/SL_fractional_left_definite.tex)：`thm:all-thresholds`, `prop:h4-core`, `thm:main`。
- [投影/有限矩修复](https://github.com/Zhongshan-Big-Jun/Sturm-Liouville-theory-research/blob/636dac87c269795a520824101f1b2abd2c583c9b/docs/SL_projection_moment_repairs.tex)：`thm:projection`, `thm:tail`, `thm:triangular`。
- [局部谱隙对称性](https://github.com/Zhongshan-Big-Jun/Sturm-Liouville-theory-research/blob/636dac87c269795a520824101f1b2abd2c583c9b/docs/SL_gap_nge2_symmetry_local_proof.tex)：`thm:r1`, `lem:away`, `thm:local`, `sec:global`。
- [带状自洽工具卡](https://github.com/Zhongshan-Big-Jun/Sturm-Liouville-theory-research/blob/636dac87c269795a520824101f1b2abd2c583c9b/tools/gap-band-extremals.md)：对称加倍段。
- [当前研究导航](https://github.com/Zhongshan-Big-Jun/Sturm-Liouville-theory-research/blob/636dac87c269795a520824101f1b2abd2c583c9b/docs/research-guide.md)：仍直接引用上述局部对称性文档，同时正确登记第六轮完整窗口。

