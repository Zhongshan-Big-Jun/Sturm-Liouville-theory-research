# Sturm–Liouville 仓库第九轮证明审计

审计日期：2026-09-23。
仓库：`Zhongshan-Big-Jun/Sturm-Liouville-theory-research`。
固定提交：`2a81b608d53e3decee04c46716a8d8f8c2d9b1b4`。
提交时间：2026-09-21 18:19:48 UTC。
提交说明：`Repair round 8 INF-limit proofs and retrieval evidence`。
上一轮基线：`d1462eb444938554b17da1db398763b31efb4ef4`。

## 1. 结论摘要

上一轮 INF 极限的主要缺口已经被新的连续解析证明实质修复，不应继续把旧二维网格的缺陷算作当前 T1 的未修复问题。本轮核对了新的无极点匹配、薄层连续相位下界、大 w 差量估计、固定参数解析展开及近极小化子论证。所查环节能够衔接。

本轮新发现集中在 B3 世俗函数的归一化，以及谱隙二阶变分的证据与表述：

1. 分块扰动程序把积分向量换成平均值向量进行欧氏投影，生成的向量一般不是其声称的切向方向。这影响 P2 的切空间数值证据。
2. 工具卡与上游研究记录以“窄脉冲使对角 Green 求和发散”为否定机制，这一说法在当前一维正则模型下不成立。正常质量缩放的脉冲有有限二阶变分极限。
3. 二阶变分推导中归一化本征函数导数的展示等式省略核分量，按字面读取错误；主要二阶特征值公式仍可保留。
4. 固定 n 的活动文稿将未归一化物理世俗函数写成严格反射对称，漏掉频率变化的比例因子。零点反射与后续已归一化的根计数不因此失效。

本轮另附平衡候选序列 $c_n(R)$ 的严格单调与精确极限证明，不混同全局上确界。

## 2. 范围与证据分层

直接阅读了当前 INF 极限 TeX、当前有理证书源代码的全部分段、修订报告和研究图；扩查固定 n 文稿、B3 的 Chebyshev/Jacobi 后续证明、相关工具卡、二阶变分程序、该程序引用的研究记录和参数表。

本轮没有重新审计全仓每个研究包，没有重跑作者全套脚本、Lean、M3/KP 的完整证明链，也没有把此前上传的讲义 PDF 当成最新仓库状态。没有写入或提交远程仓库。

`checks.py` 是独立程序，不导入仓库模块。它正常模式与 `python -O` 模式分别运行成功，20 个具名检查组得到确认。确认包括确认反例，不代表 20 个仓库定理获机器认证。负对照实际触发了除零区间拒绝及错误候选拒绝。

## 3. 上轮修复复查

### 3.1 连续薄层覆盖

来源：`docs/SL_gap_n1_inf_limit_proof.tex`，`sec:sliver`。

设 $\varepsilon=R^{-1/2}$，$\ell=1/2-u$，$w=u/\varepsilon$，$k=\sqrt\lambda$。接口射击向量为 $(\cos wk,\varepsilon\sin wk)$，轻层带来角度 $\ell k$。其连续相位

$$\Phi(k)=\ell k+A_\varepsilon(wk),\qquad
A_\varepsilon'(\theta)=\frac{\varepsilon}{\cos^2\theta+\varepsilon^2\sin^2\theta}$$

严格增加。前两个半问题根分别满足 $\Phi(k_1)=\pi/2$、$\Phi(k_2)=\pi$。导数上界与第一根下界给出

$$G(R,u)\ge\frac{\pi^2}{2\varepsilon(w+\ell)(w+\varepsilon\ell)}.$$

对 $R\ge1500,w\le2$，得到 $G>(2888/765)\pi^2>(15/4)\pi^2>3\pi^2$。它覆盖任意小正 w、旧曲边漏缝及无穷 R 尾部，不需要补跑旧矩形网格。分支选择也不再要求全域第二重层相位超过 $\pi/2$。

### 3.2 大 w 比较与 T1

来源：同文 `sec:lemAdp`、`sec:T1`。

新证明确认 $d_1>4\alpha$、$d_2<3\alpha$，其中 $\alpha=\ell/(Ru)$。粗的连续界 $B(t)\le8$ 与余切余项界已足够，得到 $G>\bar D+\ell/(Ru^3)$。

T2 给出唯一极小点及 $M<3\pi^2$。上下区域合并得到全域 $G\ge M$；固定 $u^*$ 的解析展开提供匹配上界，从而 $0\le Rm_R-M=O(1/R)$。近极小化子依靠全域下界和端点排除收敛，而非非法交换点态极限与下确界。

所复查的解析链可保留。这只直接复查明确的对称三块族，不赋予一般密度盒或 n≥2 新结论。

### 3.3 T3 证书

当前证书路径为 `research/artifacts/proof-audit-round8-20260922/certificate/certificate.py`。源码使用 Fraction 证明运算、带余项的三角多项式、Machin 区间和分母守卫；单调像采用 lower(L)、upper(H)。旧 05/16/19 的相关认证作用已明确撤回，历史字节保留。

本轮没有直接运行这一作者完整程序，而是独立重新完成 T3 的根异号、参数像、函数值和比较余量的有理包络。当前公布的 u*、M 区间得到支持。数值定位不是 T1 的必要依赖。

## 4. R9-F01：分块切空间投影错误

位置：`scripts/_gapn2_second_variation_probe.py`，`main()` 中 `favg`、`fblock` 和 P1/P2 的方向投影。

程序以 `favg[i]=integral_{I_i} f`、`fblock=favg/widths` 定义两个向量，却使用

```python
b -= b.dot(fblock) * fblock / fblock.dot(fblock)
```

使 b 正交于平均值。实际扰动函数在每块的值为 b[i]，因此正确一阶变分是 `b.dot(favg)`。二者不等价。

独立精确 SL 反例和实际 n2/R4 参数的复算见补充推导第 1 节。后者按旧规则使打印目标约为零，但实际一阶变分约为 −1.24684769078312980749。

影响：P2 关于“分块切向方向”的记录不能继续按原含义接受，修正后应重算。P1 的任意方向二阶导数比较不以切向为前提，不能据此全部否定；但其中切向残差的标签错误。P2b 使用实际积分的 gvec，是另一个分支，不能仅凭此问题一并判错。随机方向同号也不能独立证明整个切空间定号。

最小修复：明确系数度量，使用正确积分法向量（或带宽度度量的平均值法向量）；独立检验实际积分残差，并处理法向量为零的情形。

## 5. R9-F02：不存在所声称的必然 Green 对角发散

位置：`tools/second-variation-weighted-eigenvalues.md` 的“宽度路径的 delta' 边界层”段，以及
`runs/rigorous-open-math-research/R-20260812T090000Z-g1prime-g2/run_notes_addendum_2026-08-13c.md` 的 `NEGATIVE` 段。

二者均用窄脉冲导致求和发散，作为否定二阶变分衔接路线的理由。

常密度例子中，单位总质量脉冲集中到 1/2 时，$\lambda_1''\to6\pi^2$、$\lambda_2''\to0$，故 $Q=\tfrac12D_1''\to-3\pi^2$。谱和由可求和上界控制，解析证明不依赖数值截断。

对固定有界正密度、有限层结构，一维 Green 核去掉简单极点后的有限部在空间变量上连续；固定总变差的集中脉冲二次型因此也保持有限。不能将其他维数、核导数或不同质量缩放下的发散搬到此处。

应保留的正确部分：移动接口并非线性密度路径，第二路径导数带 $\delta'$，产生 $-\sum_i s_i d_i^2 f'(a_i)$ 这一有限主阶项。删除“必然发散”理由，不意味着忽略这项或宣布线性密度 Hessian 等于宽度 Hessian。也不能据此宣布 G1' 的符号问题已经解决。

## 6. R9-F03：归一化导数的展示等式少了核分量

位置：上一工具卡与研究记录的推导第 4 步。

文字承认核分量由约束决定，但展示等式直接将 v 等同于普通 L2 约化逆的像。取 `dr=rho`，其右端为零，而加权归一化的真实导数为 $v=-u/2$。

这是需要澄清的公式/规范问题，不推翻主要 $\lambda''$ 公式。应使用加权本征展开给出核项，或明确仅在模核意义下使用约化逆的特解。精确修正式见补充推导第 2 节。

## 7. R9-F04：物理世俗函数的反射等式漏了比例因子

位置：`docs/SL_fixed_n_supremum.tex` 的 `thm:sym` 及其 J 共轭证明；同文 $F_n=\sin y Q_n(\cos y)$ 的未归一化写法也应同步。

按该文自己的定义 $y=\omega s t_n$，物理函数 $F_n=(T_{end}T_{cell}^n)_{01}$ 含一个 $1/\omega$。反射 y 时不能冻结 omega。准确关系为

$$F_n(\pi-y)=\frac{y}{\pi-y}F_n(y).$$

$n=1,R=4,y=\pi/3$ 给出严格不相等的两个函数值，见补充推导。

最小修复：定义 $\widehat F_n=\omega F_n$，再对其使用反射、三角多项式与 Chebyshev/Jacobi 表达。其余正比例关系说明根反射和单根结构不变。

重要边界：后续 `runs/plugin-perf-eval2/R-20260822T220000Z-b3-baseline/candidate_proof.md` Part B 已明确写出 $G_n=\omega F_n$ 并说明两者零集相同，所以不应把旧活动文稿的函数值错误夸大为后续 2n 根计数失效。

## 8. 建设性补充

补充推导第 5 节用嵌套 Jacobi 矩阵证明规定平衡候选序列 c_n(R) 严格递减，并得到精确极限

$$c_\infty(R)=\left(\frac{\pi-\varphi(R)}{\varphi(R)}\right)^2,
\quad\varphi(R)=\arccos\frac{\sqrt R-1}{\sqrt R+1}.$$

这一部分在当前 `tools/bloch-band.md` 中仍标数值观察。可以在独立复核本补证后，更新候选序列的状态；全局最优性 O1/O2 与整个 $\Lambda_n^{sup}$ 的结论不随之完成。

## 9. 建议次序

优先修正 P2 的切空间投影并重跑受影响证据；撤回 Green 对角发散这一错误否定理由，同时保留并精确解释接口加速度项；统一本征函数导数规范和世俗函数归一化。已经由连续估计修好的 INF 极限主链应当保留。

本轮不修改数学结论的范围，不要求为这几个局部问题重新优化整个项目，也不把模型输入表的小数当成严格驻点证书。
