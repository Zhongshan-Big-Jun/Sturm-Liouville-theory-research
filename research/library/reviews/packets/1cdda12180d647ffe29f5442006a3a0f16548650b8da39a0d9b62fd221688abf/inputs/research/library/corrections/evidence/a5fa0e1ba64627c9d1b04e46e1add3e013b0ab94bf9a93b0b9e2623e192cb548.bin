# Sturm–Liouville 仓库第三轮证明审计

审计日期：2026-09-20  
仓库：`Zhongshan-Big-Jun/Sturm-Liouville-theory-research`  
固定提交：`aac44f5a5831d9c4b7140027a09a06e8af4250f5`  
提交说明：`Repair audited proofs and publish reviewed tool-library corrections`  
前一审计基线：`ee90dcc2390eed8586888c07eaaabced37e3226d`

## 结论

本次更新包含实质性的数学和程序修复，不应把前两轮发现原封不动地继续列为当前错误。本轮核验了新增首对比值的全局变分论证、MW 的若干关键修补、修订后的谱系和幂域处理、C1 有理包络修正及 K1 终端条件，并实际运行了两段字节身份与当前提交一致的谱验证脚本。

扩查仍发现两组可以被反例直接否定的稳定性结论，以及一处可以局部修补的商空间完备性证明缺口：

| 编号 | 位置 | 判断 |
|---|---|---|
| R3-F1 | `docs/SL_stability_moment_jump.tex`：`thm:sharp`、`cor:di`、`thm:line`、`thm:model`；`tools/jump-stability.md` | 一般二阶递推的乘积下界被误写成等式，错误推广到增长分类和不完备性；结论按目前范围为假 |
| R3-F2 | 同文档 `thm:margin` 第 (ii) 项；同工具卡的有界基扰动声明 | 漏算扰动对二阶导数系数的影响；有界扰动不足以保证完备性；给出非零正交元反例 |
| R3-F3 | `docs/SL_krein_c0_limit.tex`：`thm:complete` 第 (d) 项的证明 | 商空间等距映射后的函数被误认，现有推理不成立；结论可通过谱截断保留 |

这不是对整个仓库的全面认证，也不是对全部主要结论的否定。本轮没有完整重审 M3/KP、一般 n>=2 极值分类和全仓库历史研究包，没有重跑 Lean 或全量构建。

## 一、前两轮关键修复的核验

### 1. 谱系、谱投影程序和域判定

当前 `docs/SL_fractional_left_definite.tex` 已补入特征值 c 的线性模态 x，明确其重数为二；正弦模态的 L2 范数平方改为 mu²/(1+mu²)。负阶空间采用弱谱范数下的完备化，而非把 L2 配上不等价弱范数后直接当作完整 Hilbert 标度。

同文档正确保留 H3 主定理和谱截断推出的 0<=s<=3 稠密性，并给出：

\[
x^2\in H^t\iff 0\le t<3/2,\qquad
p_4\in H^s\iff 0\le s<7/2.
\]

其中 p4 的成员阈值不是整个族在 3<s<7/2 中稠密的证明；当前文稿已作此区分。前两轮的反例现在已经纳入正确的范围说明，因此不能继续将其当作当前文稿重复犯错的证据。

本轮实际执行：

- `scripts/op10_verify_mechanism.py`：退出 0。
- `scripts/op10_fractional_window.py`：退出 0。

原脚本的全部输出保存在 `results/`。执行副本由 GitHub 连接器返回的全文复原，并在执行前验证 Git blob SHA-1：

| 文件 | 当前提交的 Git blob | 本轮核对 |
|---|---|---|
| op10_verify_mechanism.py | e417904b1f0203afeefbe4fb4ad3d344e39dc4e0 | 一致 |
| op10_fractional_window.py | a36c07dfddf0b3d8ceffa3c155b98432bf19c62b | 一致 |

额外使用 90 位运算的独立极点距离二分，比较两段程序在 k=1,2,3,4,40,1000,1000000 的结果；最大误差约 1.04487043696 个浮点 ULP。k=10^12 的不可分辨根被明确拒绝。线性模态在 c=3、t=1 时的范数平方恢复为 2。

这些是实际运行与数值诊断，不是向外舍入证书，也不是无限维稠密性的证明。

### 2. 首对比值和 MW 重证

`docs/SL_ratio_proof.tex` 的新增首对全局极值部分，现在从紧性和一阶变分出发，依次推出切换规则、商函数单调性、零能量、两端斜率关系、对称性及平衡宽度。该论证没有再把候选配置的特征值计算冒充全局上界。本轮阅读和推导核对未在这条新增主链中发现实质缺口。

`docs/SL_mw_lemma_reproof.tex` 的已核对部分修正了 c² 的仿射缩放，使用有符号端点斜率传输系数，以参数 Wronskian 推出零点运动，并显式处理共同零点的等号分支。其倍指标论证按首对上、下确界定义常数，不以首对闭式为前提，因而与新增首对变分论证没有原来那种交叉依赖缺口。

这是一项对具体修补和所阅读证明链的判断，不涵盖仓库中每一份比值问题历史草稿。

### 3. C1 包络、good-root 归一化和 2n 能量公式

当前 `_symline_allR_certificates.py` 使用 S13/C10，而不是原来的 S13/C8，并对 Taylor 截断项数奇偶性、定义区间及分母正性进行检查。新硬编码分数与 S13/C10 精确一致。本轮独立有理数复算了 C1 两端点链及 C2–C5 的所列有理比较，全部成立。没有宣称重跑全部历史区间证书或 n=1 全链机器验证。

`SL_gap_n1_global_goodroot_proof.tex` 的 `lem:struct` 已改用

\[
\widehat y_2/\widehat y_1=\sqrt{n_1/n_2}\,y_2/y_1,
\]

并只使用“节点两侧各至多一个零点，已知两个零点则分居节点两侧”的正确推论。

`SL_gap_nge2_exact_2n_switches_proof.tex` 的 `eq:blockconst`、`eq:potential` 已补齐导数与特征值因子，修正后的能量计算给出 K=-2D。本轮核对了这些修订位置，没有重新全面认证整篇定理的所有背景依赖。

### 4. K1

当前 `SL_third_order_K1_proof.tex` 明确

\[
d_N^{(N)}=v_{N-2}^{(N)}=\frac{N}{(N+1)(2N)!}>0.
\]

本轮以精确分数核对 N=3,...,30 的 462 个有限公式项和 28 个终端条件，与向后递推一致。当前有限公式及正项级数极限论证中，本轮未发现新的实质问题；这不是对 e/4 极限的 Lean 形式化。

## 二、R3-F1：一般二阶递推的乘积下界并不是等式

### 原断言与失效步骤

稳定性文稿的增长引理本身给出正确下界。设

\[
c_0u_m=A_mu_{m-1}-B_mu_{m-2},\quad u_0=0,\quad u_1=1,
\qquad \varepsilon_m=\frac{A_m-B_m-c_0}{c_0}\ge0.
\]

准确的分解是

\[
u_m=(1+\varepsilon_m)u_{m-1}
 +\frac{B_m}{c_0}(u_{m-1}-u_{m-2}).
\]

第二项非负，通常并非零。因此只能得到

\[
u_m\ge\prod_{k=2}^m(1+\varepsilon_k).
\]

但 `thm:sharp` 在只假定 B_m>=0 时，直接把 epsilon_m=C/m 写成上述乘积的精确解；`thm:line` 同样直接使用 log(u_m)=sum log(1+epsilon_m)。`cor:di` 进一步把 S(m)=O(log m) 与 u_m 至多多项式增长视为等价。这些推断都漏掉了上面的第二项。

### 反例 A：直接否定“尖锐性”定理的精确乘积及不完备性结论

取两种奇偶递推都为

\[
c_0=1,\qquad B_m=2,\qquad A_m=3+\frac2m.
\]

完全满足原定理的 B_m>=0、A_m-B_m=1+2/m 条件。但

\[
u_2=4,\qquad \prod_{k=2}^{2}(1+2/k)=2.
\]

错误在第一个递推步骤已出现，而不是只有渐近误差。

令 d_m=u_m-u_{m-1}，则

\[
d_m=2d_{m-1}+\frac2m u_{m-1}\ge2d_{m-1},\qquad d_1=1.
\]

归纳得 d_m>=2^(m-1)，于是

\[
u_m\ge2^m-1.
\]

它并非原文声称的 m²/6 量级。以文稿的对角空间 H_beta 为例，任何正交元的偶、奇矩分别为 M2*u_m、M3*u_m，而 Cauchy–Schwarz 给出的矩界至多为常数乘 m^beta。因此 M2=M3=0，所有矩为零；由多项式稠密性，正交元为零。

故这个族对每个固定有限 beta 都完备，特别是 beta=3 时完备，直接反驳原文 C=2、beta>C+1/2 时不完备的结论。此处 H_beta 是系数对角空间，不是 Krein 左定记号 H^s。

### 反例 B：直接否定“二分”推论

取

\[
c_0=1,\qquad A_m=3,\qquad B_m=2.
\]

则 epsilon_m=0，S(m)=0，却有精确解

\[
u_m=2^m-1.
\]

同理，奇偶两族采用这些系数时，在每个多项式范数增长的对角空间中均完备。因此 S=O(log m) 不能推出存在某个 H_beta 使其不完备，也不能推出实际解有多项式上界。

门槛线 epsilon_m=C/(m log m) 也有同样问题：仍取 B_m=2、A_m=3+epsilon_m，差分满足 d_m>=2d_(m-1)，故实际解至少指数增长，而不是仅由 epsilon 指定的对数幂增长。

### 可保留与必须修改

可保留：定量增长下界、明确要求两种奇偶递推都满足相应条件的充分性论证，以及使用实际解 u_m、u'_m 的对角级数充要判据。

必须修改：对一般 B>=0 的“尖锐性”“二分”“门槛线完全分类”等超出证明范围的断言。乘积模型可通过明确附加 B_m=B'_m=0 保留；此时从 k=2 起乘的归一化常数为 Gamma(2+C)，而 `tools/jump-stability.md` 还残留 Gamma(1+C)。

`cor:di` 可以改为以实际解的上界为假设，例如若某个奇偶递推满足 u_m<=C*m^D，则 beta>D+1/2 时相应对角级数收敛，从而不完备。不能把这个上界从 S=O(log m) 单独推出。

另外，S=O(log m) 与 S=omega(log m) 并不穷尽所有非负增量序列；本轮不提供一般门槛分类的完整替代定理。

## 三、R3-F2：基系数扰动漏算 B_m；有界扰动可以破坏完备性

### 正确系数

原文 `thm:margin` 第 (ii) 项考虑

\[
\widetilde p_{2m}=x^{2m}-\left(\frac m{m-1}+\delta_m\right)x^{2m-2}.
\]

先只把 L_poly=c-D² 当作多项式上的微分表达式。其系数是

\[
A_m^\delta=2m(2m-1)+c\left(\frac m{m-1}+\delta_m\right),
\qquad
B_m^\delta=\left(\frac m{m-1}+\delta_m\right)(2m-2)(2m-3).
\]

所以

\[
\boxed{A_m^\delta-B_m^\delta
=4m+\frac{cm}{m-1}
+\delta_m\bigl[c-(2m-2)(2m-3)\bigr].}
\]

原文只保留 c*delta_m，漏掉了负的二阶导数贡献。取 c=3、delta_m恒等于1，在 m=4 时，真实 A=63、B=70，差为 -7；原文的下界表达式给出 23。故这个推断不是可以通过微调常数挽救的估计。

此外，若仍使用真正的 Krein 算子 K_c，则这些扰动多项式为偶函数且

\[
\widetilde p'_{2m}(1)=-(2m-2)\delta_m.
\]

只要 delta_m不为零，就已不满足偶函数的 Krein 边界条件。形式微分表达式与真正算子作用必须再度区分。

### 真正的不完备性反例，而非仅仅“无法套用旧引理”

即使退回原文使用的抽象多项式框架，选择 H=L²(-1,1)，有界扰动也不足以保持完备性。

固定 c=3，令

\[
g(x)=P_2(x)=\frac{3x^2-1}{2},\qquad \|g\|_2^2=\frac25>0.
\]

直接积分得到

\[
\mu_{2m}=\langle g,x^{2m}\rangle
=\frac{4m}{(2m+1)(2m+3)},
\]

以及

\[
\nu_{2m}:=\langle g,L_{\rm poly}x^{2m}\rangle
=-\frac{4m(4m^2+2m-9)}{(2m+1)(2m+3)}.
\]

所有 m>=1 的 nu_(2m) 都非零。对 m>=2 定义

\[
\alpha_m=\frac{\nu_{2m}}{\nu_{2m-2}},\qquad
\widetilde p_{2m}=x^{2m}-\alpha_mx^{2m-2}.
\]

则其相对原系数的扰动为

\[
\boxed{\delta_m=\alpha_m-\frac m{m-1}
=\frac{6m(2m+5)}{(m-1)(2m+3)(4m^2-6m-7)}.}
\]

这里 delta_2=-36/7、delta_3=1。对 m>=3，分母为正，而且

\[
1-\delta_m
=\frac{(m-3)(2m-1)(4m^2+10m+7)}{(m-1)(2m+3)(4m^2-6m-7)}\ge0.
\]

因此整个扰动序列有界：-36/7<=delta_m<=1，且 delta_m~3/(2m²)。并未仅仅满足单侧有界，它还趋于零。

令 q0=3、q1=3x，偶族 q_(2m)=L_poly(tilde p_(2m))，奇族仍用原始奇多项式的形式像。按 alpha_m 的定义，所有偶族都满足

\[
\langle g,q_{2m}\rangle=\nu_{2m}-\alpha_m\nu_{2m-2}=0.
\]

g 为偶函数，故也正交于奇族；又正交于 1、x。因此 g 是整个 q 族的非零正交元，族在 L² 中不完备。

这否定的是“仅凭基系数有界或有界下界扰动就保持完备性”的声明，不否定附加了正确 B>=0、A-B>=c0 等条件的充分性定理。本例的意义正是：这些条件不会由文中所给扰动条件自动得到。

### 最小修正

先替换准确的 A_delta、B_delta 差式，再分清 H²/H³ 的真正算子域设置与一般 Hilbert 空间上的形式多项式设置。对扰动后的全部下标明确要求实际系数满足增长引理的条件；只有“从某个大下标开始满足”时，还必须证明前面有限步骤没有消除增长分量。撤销无条件的“有界基扰动保持完备性”，同步更正工具卡。

## 四、R3-F3：商空间完备性的证明用错等距映射

位置：`docs/SL_krein_c0_limit.tex`，`thm:complete` 第 (d) 项证明。

文中此前正确给出等距映射

\[
T([f])=f'-\frac{f(1)-f(-1)}2.
\]

因此真正的像为

\[
T([\cos(n\pi x)])=-n\pi\sin(n\pi x),
\qquad
T([\sin(\mu_nx)])=\mu_n\cos(\mu_nx)-\sin\mu_n.
\]

但第 (d) 项证明却把正交条件写成测试 cos(n*pi*x) 与 sin(mu_n*x)，而不是上述像，并以原 L² 谱系完备性收尾。这里既误用了函数，也没有得到对遗漏的线性模态 x 的正交性。

取 h=x，即有积分均值为零，且

\[
\int_{-1}^1 x\cos(n\pi x)\,dx=0,\qquad
\int_{-1}^1 x\sin(\mu_nx)\,dx=0
\]

（后一式由 tan(mu_n)=mu_n 得到）。所以非零 h 满足该段实际写出的全部条件，不能由这些条件推出 h=0。

这不是该完备性结论的反例：h=x 并不正交于真正的 T 像，例如

\[
\int_{-1}^1xT([\cos\pi x])\,dx=-2\ne0.
\]

### 可以保留结论的修补

固定任意 c>0。对 f 属于第一左定空间，K_c 的有限谱截断 f_N 在 H1 的左定范数中收敛。商映射满足

\[
\|[f-f_N]\|_{1,0}\le\|f-f_N\|_{1,c}\to0.
\]

常数与 x 两个模态在商空间中为零，故 [f_N] 正是所列正谱特征函数像的有限线性组合。因而它们在商空间中稠密。这段谱截断论证可以直接替换原来的错误段落，无需撤回商空间完备性结论。

## 五、验证范围和可复现说明

本轮通过 GitHub 连接器读取固定提交。容器内直接克隆因无法解析 GitHub 域名失败，因此没有声称取得完整仓库或执行其全部测试。两段需要执行的脚本由连接器全文复原，逐字节的 Git blob 校验一致后才运行。

`check_round3.py` 中的独立核验分为符号恒等式、精确有理数反例与比较、有限递推、源码身份检查和数值诊断。结果文件记录 34 项全部符合预期。注意其中包含对错误命题的反例确认；这一数量不是被认证的定理数。

- 稳定性反例的普遍性由本报告的精确公式和归纳承担，不靠有限扫描。
- 有界扰动反例的全指标正交性来自 nu_(2m)-alpha_m nu_(2m-2)=0；22 个直接多项式积分只作交叉检查。
- 根求解对照是 90 位数值计算，不是向外舍入区间证书。
- 当前两个脚本输出已主动注明有限投影与无限维结论的区别，本轮没有将其自测成功升级为数学全认证。
- 没有更改、推送或提交任何仓库文件。
- 没有重跑 Lean，没有审核完整 M3/KP、全量 PDF 构建或全部历史证据包。

## 六、修复顺序

首先修稳定性文稿的结论范围：保留正确下界和充分条件，把精确乘积模型限制为 B=0，删除由 S 上界推实际解上界的无效推论。其次修真实扰动系数及其前提，撤回无条件稳健性，并同步 `tools/jump-stability.md`。再次替换 c->0 文稿的商空间谱像证明。

前两轮已修好的谱系、低阶分数稠密性、首对全局极值步骤、C1 包络和 K1 终端修补不应因本轮的不同支线发现而被一并撤回。
