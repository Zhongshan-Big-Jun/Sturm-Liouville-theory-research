# Sturm–Liouville 仓库第十轮审计

审计日期：2026-09-25。仓库：`Zhongshan-Big-Jun/Sturm-Liouville-theory-research`。

固定提交：`5bb6b2605122d2daf472863b4bec0f35a9c192e1`，提交时间 `2026-09-23T14:07:53Z`。结束前重新读取主分支提交，仍为此版本。没有修改或提交仓库文件。本轮不以会话中上传的讲义 PDF 代替仓库源码。

## 结论

本轮重点复查上一轮的二阶变分修订，并审查最新文献吸收中新增的域刻画、三阶删项闭包、相容 Legendre 替代系、有限 TSVD、无限删除子类和局部界面二阶公式。在所检查的证明步骤和外部定理适配中，未发现需要撤回这些新增主要结论的实质性缺口。

确认两个新的程序／实验设计问题：

| 编号 | 位置 | 问题 | 性质与优先级 |
|---|---|---|---|
| R10-01 | `scripts/_gapn2_symmetry_recon.py::roots_of`；被修订后的二阶变分程序继续调用 | 固定步长世俗函数变号扫描漏掉低频根，却将更高根近似值标为前若干根 | 高优先级；真实计算错误，可使谱序号和相应变分对象错配 |
| R10-02 | 同文件 `main()` 的 `asym` 扰动构造 | 一次全索引反转赋值不是反射扇区投影 | 中优先级；不能据该组试验声称专门检查了某个纯反射扇区 |

这两个问题都不是现有解析对称性定理、完备性定理或新 Riesz 替代系定理的反例。第一项也没有证明默认 R=4 的既有驻点表已经算错；本轮给出的是共享求根器所承诺输入范围内的明确失败实例。

## 1. 上一轮二阶变分修订复查

当前 `scripts/_gapn2_second_variation_probe.py` 已将切向法向量改为真实块积分 A_i=∫_{I_i}f，并在主循环中实际把 `Tangent.A` 传入 `project_tangent`。宽度加权度量也单独处理，另有直接积分残差，而不只是输出构造时强制为零的内积。

`research/artifacts/proof-audit-round9-20260923/analytic-repair.md` 已恢复归一化本征函数导数的核分量，区分加权 Green 有限部与普通 L² 正交约化逆，并以有限核的集中极限取代旧的无条件“对角发散”理由。移动接口的路径二阶项仍被保留。

最新 `interfaces/derivations/P3-interface-chain-rule.md` 进一步在有限、固定块值、不碰撞接口的明确范围内给出局部证明，而不是假定任意分布路径上的 Fréchet 二阶可微性。

上述旧缺陷不再作为当前版本的新增错误登记。

## 2. R10-01：根残差正确并不保证谱序号正确

### 2.1 源程序的承诺与实现

`roots_of(blocks,k)` 的说明是返回物理传递矩阵 M01 的前 k 个正根，即前 k 个特征频率。实现却先在一个均匀网格上寻找端点变号，随后仅对找到的前 k 个变号格二分。

网格数至少 20000，并随上界扩大到大约 0.02 的频率步长。两个简单根若同处一格，端点可以同号，这一格就被完全跳过；增加二分次数不改变这一问题。

修订后的 `checked_roots` 只检查返回个数、有限性、正性与递增性。`HighPrecisionTangent` 从同一列表取得初值，再局部高精度细化和检查残差，因此没有独立检查谱序号。`SpectralProbe` 与有限差分路径也继承该枚举器。

源程序已经将这些计算标为非区间数值证据。这一点应当保留。但“没有严格证书”和“数值上把第五个或更高模态错当第一个”是不同问题，后者需要修正。

### 2.2 明确反例

在 [0,1] 上取

\[
\rho(x)=\begin{cases}
10000,&0<x<1/50\text{ 或 }49/50<x<1,\\
1,&1/50<x<49/50.
\end{cases}
\]

这是合法的正密度三块问题。设 ω=√λ。利用中心反射，半区间的两个无极点匹配函数为

\[
E(\omega)=\cos(2\omega)\cos(12\omega/25)
-\tfrac1{100}\sin(2\omega)\sin(12\omega/25),
\]

\[
O(\omega)=\tfrac1{100}\sin(2\omega)\cos(12\omega/25)
+\cos(2\omega)\sin(12\omega/25).
\]

初始条件 y(0)=0,y′(0)=1 时，半区间末端状态为 (O/ω,E)。全区间世俗函数为 2EO/ω。以下四个互不相交的有理区间，各自经精确 Taylor 余项包络认证含有一个特征频率：

| 半区间条件 | 左端 | 右端 | 对应函数端点符号 |
|---|---:|---:|---|
| E=0 | 0.783424012239 | 0.783424012240 | +,− |
| O=0 | 0.797807654414 | 0.797807654415 | +,− |
| E=0 | 2.345707847544 | 2.345707847545 | −,+ |
| O=0 | 2.358540421082 | 2.358540421083 | −,+ |

这里的认证证明四个根存在且不同，已经足以否定下述返回值的“前 k 根”身份；本轮反例不依赖另外证明它们恰为前四根。

按源函数逻辑独立复现，`k=3` 返回：

```
3.2671480773046255
3.925370522341491
3.94200553547788
```

还有一个更简短的独立检查：取试探函数 sin(πx)，由 ρ≥1 得 λ₁≤π²，故真正的第一特征频率必不超过π。而程序首项3.267148…甚至大于22/7>π。

最大世俗残差约为 1.63×10⁻¹⁷。返回列表满足个数、正性与严格递增要求，却把四个已认证更低的根全部漏掉。`k=2` 和 `k=61` 的首个返回值同样是 3.2671480773…。

对 `k=3`，第一对低频根同处网格格子

```
[0.7800080064433922, 0.8000082117111715]
```

两个格端的世俗函数符号均为正。本轮用这些浮点格端的精确二进制有理值重检了符号，也用输入块宽 `.02`、`.96` 的精确有理表示重检了上述四个根括区；结果并非十进制／二进制转换造成。

### 2.3 修复要求

需要在局部求根之前独立控制根的序号。可采用具有连续提升的 Prüfer 相位或其他 Sturm 计数，为每个目标指标建立括区，再细化根的位置。对于单位区间和 1≤ρ≤R，比较界 nπ/√R≤ω_n≤nπ 可提供初始尺度，但相邻比较区间可能重叠，不能替代计数。

把上述高反差近邻根案例纳入回归测试，要求至少核对前若干指标，而不只是方程残差。更细的固定网格、更多保留模态、更高浮点精度，都不能单独成为无漏根的保证。

影响应沿实际调用追踪。不能从这个实例宣布所有默认参数报告失效；也不能继续把共享同一枚举器的两份计算当作相互独立的谱序号验证。

## 3. R10-02：反射扇区并未按注释构造

源 `main()` 的 `asym` 分支执行：

```python
a = rng.standard_normal(2*n)
a[2*n-1-np.arange(2*n)] = -a
```

设 J 是坐标反转矩阵。这个全索引赋值仅把原向量 v 变成 −Jv，不是求满足 Jd=±d 的投影。

输入 (1,2,3,4) 后结果为 (−4,−3,−2,−1)，既不满足 Jd=d，也不满足 Jd=−d。NumPy 实际执行与该代数判断一致。

有序接口的几何反射为 R(x)=1−Jx，因此在对称基点的微分为 −J。应区分：

\[
d_{\rm preserve}=\tfrac12(v-Jv),\qquad Jd_{\rm preserve}=-d_{\rm preserve},
\]

\[
d_{\rm break}=\tfrac12(v+Jv),\qquad Jd_{\rm break}=d_{\rm break}.
\]

第一种保持镜像成对；第二种是反射作用的纯破缺方向。零投影应重新取样。步长还须保证接口次序；转换参数后应检查实际扰动，不应让不可行方向被裁剪后继续沿用原标签。

同文件后面的 `sym` 分支使用 `concat([a,-a[::-1]])`，这正是保持镜像对称的方向，不应当一起判错。仓库也有普通随机种子搜索；本项不意味着完全没有探索非对称方向，只是所谓纯反射扇区试验尚未按标签实现。若只需要普通随机方向，应改正注释和证据描述。

## 4. 新文献吸收的数学内容：本轮复核意见

### 4.1 奇偶幺正分解与分数域

`domains/proofs/01-parity-unitary.md` 明确保留 √2 范数因子，反向检查零点拼接，并把定义域一起传输到半区间 Neumann–Neumann 与 Dirichlet–Robin 实现。

`02-fractional-trace-dictionary.md` 没有把 Grubb 的统一阶数边界定理直接套在未说明的混合阶数半区间。它改用全区间 Neumann 和正移位 Robin 辅助算子，再限制奇偶子空间、比较正标量移位。正性、两向域包含、临界加权条件和低阶余项的 Hardy 控制能够衔接。

本轮对照了 Grubb 1412.3744v4 第2节的边界问题假设、Definition 2.1、Theorem 2.2 和 Corollary 2.3；没有对整篇外部论文进行独立重证。该方法的范围仍是明确的光滑常系数模型、固定 c>0，不包括任意边界实现。

### 4.2 三阶余有限闭包

`03-s3-cofinite-closure.md` 的关键新增点不只是 f″(0) 连续的必要性。它确实证明了高阶完整尾部在三零迹空间中稠密：使用局部量 ∥f‴∥_{L²(-2δ,2δ)}→0 完成截断，再固定 δ 作可整除多项式逼近与端点修正。未发现错误的极限交换。

因此，所检查证明支持：在 H_c³ 中，余有限子族的闭包由是否保留 p0,p1,p4 分别决定 f(0),f′(0),f″(0) 的约束。删除 p4 在二阶仍稠密、三阶却留下超平面，并不矛盾。这一结果没有被当作 s=5/2 的临界结论或任意无限删项分类。

### 4.3 Legendre 替代系与有限 TSVD

`approximation/notes/03_legendre_krein_construction.md` 从 Jʳe_n 的高阶导数正交性出发，补入固定低阶提升函数，得到 s=2、4 的实际幂域分解和度数无关的固定-c Riesz 上下界。其证明并不只依赖有限 Gram 矩阵的观察。

本轮精确核对了相容低阶提升函数、部分高阶列的端点条件、带宽为零实例，以及所列 M_c² 的完整多项式恒等式。有限样本不承担全指标 Riesz 证明；该证明依赖文稿中的函数空间分解及双向范数估计。未发现这些步骤的实质性缺口。

`02_finite_tsvd.md` 则在有限合成映射中直接证明投影、√ε 误差与 ε⁻¹ᐟ² 矩噪声估计，并区分 Gram 与合成映射的截断尺度。对照 Adcock–Huybrechs v4 的相关定理，未发现把无限 frame 前提偷偷省略后继续使用无限极限的情况。

这些结果研究的是重新构造的相容函数族，不能解释为原稀疏单项式差族突然成为 Riesz 基，也不提供 c→0 一致常数。

### 4.4 无限删项子类

`04_infinite_deletions.md` 的等差数列正面结论通过平滑截断和 b_m′(t)=m(t−1)t^{m−2} 的精确积分构造证明；不是将全 Müntz 定理直接套到差分多项式。

倒数可求和子类则先证明扩大后的单项式空间不稠密，再取得真实 L² 正交向量，沿奇偶幺正映射及 K_c⁻¹ 返回 H_c²。这个推理方向正确。外部 Müntz 定理的指数下界、去重和权变换与原文 Theorem 1.3 相符。

因此等差型保留集的稠密性和 dyadic 保留集的不稠密性应与一般发散、非等差指标集合仍未分类的状态分开。

### 4.5 有限移动接口二阶公式

`interfaces/derivations/P3-interface-chain-rule.md` 首先用传递矩阵和简单根证明局部解析性，再证明归一化本征函数一次 H¹ 可微，随后通过接口处材料导数得到二阶特征值公式。没有要求通常不成立的本征函数全局 C³ 或二次 H¹ 可微。

Green 核的规范、归一化核分量、几何 d_i² 项及路径加速度 e_i 项均被保留。本轮对单接口 1/4 密度例的两个模态进行了70位隐式求导交叉检查，支持原文的符号与因子。

该局部公式不证明全局 Hessian 定号、G1′、有限反差全局唯一性或接口碰撞极限。

## 5. 实际执行与未覆盖范围

独立程序 `checks.py` 在普通模式与 `python -O` 下各成功运行，均确认23个具名检查组。检查混合了精确有理符号、符号多项式恒等式、有限输入的程序执行和70位数值导数对照；“确认”包括确认源程序失败，并不是23个仓库定理得到机器认证。

`probe_roots.py` 是依据读取到的 `roots_of`／`D_scalar` 编写的语义复现；不是字节相同的完整作者源码副本，也没有运行作者整个脚本主流程。没有运行原仓库完整测试套件、Lean、M3/KP 或所有历史区间证书。没有作新的全局谱优化结论。

开发中一次扩充二进制精确包络时，巨大有理数字符串触发 Python 的默认4300位转换限制；修正本地证书序列化配置后，两个模式均重新执行成功。该问题不属于仓库错误，也未被计为数学失败。最终日志与各自结果文件保留在包中。

## 6. 修复顺序

先修共享求根器的谱序号识别，再逐项追踪并重算受影响的数值证据；然后明确反射扰动的两个扇区，修正相应试验标签与回归检验。最新域刻画、三迹闭包、相容 Riesz 替代系等已经通过本轮关键步骤复核的内容，不应因上述诊断程序问题而被撤回。

## 7. 主要来源定位

所有仓库路径均相对于固定提交 `5bb6b2605122d2daf472863b4bec0f35a9c192e1`：

- `scripts/_gapn2_symmetry_recon.py`：`roots_of`、`D_scalar`、`Recon.f_at`、`main` 的两个扰动分支。
- `scripts/_gapn2_second_variation_probe.py`：`checked_roots`、`HighPrecisionTangent`、`SpectralProbe`、`project_tangent`、`run_probe`。
- `research/artifacts/proof-audit-round9-20260923/analytic-repair.md`：V1–V4。
- `literature/absorption-20260923/domains/proofs/01-parity-unitary.md`：P1–P4。
- 同目录 `02-fractional-trace-dictionary.md`：D1–D5。
- 同目录 `03-s3-cofinite-closure.md`：C1–C8。
- `literature/absorption-20260923/approximation/notes/02_finite_tsvd.md`：T1–T4。
- 同目录 `03_legendre_krein_construction.md`：L0–L4。
- 同目录 `04_infinite_deletions.md`：D0–D10。
- `literature/absorption-20260923/interfaces/derivations/P3-interface-chain-rule.md`：P1–P9。

外部核对为：Grubb `arXiv:1412.3744v4` 第2节；Adcock–Huybrechs `arXiv:1612.04464v4` §5；Erdélyi–Johnson 作者稿 `bill.pdf` Theorem 1.3。取得原文不等于逐式审计整篇论文。本轮未核实所有13篇吸收文献的全部引用。
