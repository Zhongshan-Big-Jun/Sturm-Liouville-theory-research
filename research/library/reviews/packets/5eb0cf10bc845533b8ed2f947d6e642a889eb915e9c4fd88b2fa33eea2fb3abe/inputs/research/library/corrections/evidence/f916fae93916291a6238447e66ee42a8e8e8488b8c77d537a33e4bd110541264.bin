# Sturm–Liouville 仓库第二轮独立证明审计

**审计日期：2026-09-20。**

**仓库：** `Zhongshan-Big-Jun/Sturm-Liouville-theory-research`。

**固定提交：** `ee90dcc2390eed8586888c07eaaabced37e3226d`。通过 GitHub 连接器重新读取最近提交，最新提交仍为 2026-09-09 的这一版本。没有向仓库写入、提交或修改文件。

本轮不是重复上一轮问题清单：重新核算上一轮的 p4 定义域反例与 C1 证书反例，并扩查首对及倍指标比值、分数阶空间、相关谱计算程序、K(1)=e/4。没有重跑整个仓库、全部区间证书、M3/KP 研究包或 Lean 构建。

## 总结

不能给当前仓库整体通过的结论。新发现中最实质的不是排版，而是分数阶支线的验证对象有误：漏掉真实特征函数、使用错误归一化、把发散的谱级数截断值当成范数、把不完备的负阶内积空间当成 Hilbert 标度。另一个仍被文档引用的求根脚本从第三个正根开始返回非根。

比值闭式没有因此被推翻，但两篇宣称自足的证明遗漏了首对比值的全局最优性步骤。本轮另外给出该步骤的变分修复证明，见 `ratio_first_pair_repair.md`。K(1)=e/4 的核心分解及极限论证能够保留，其一处差分初值应更正。

## B01：分数阶验证脚本漏掉 x，并错误归一化正根正弦模态

**来源：** `scripts/op10_verify_mechanism.py`，`basis()`、`inner_t()`；`docs/SL_fractional_left_definite.tex`，第 1 节谱描述及数值验证段。

该脚本的谱基只有常数、cos(nπx) 与 sin(μₙx)，其中 μₙ>0 满足 tan μₙ=μₙ。它遗漏了特征值 c 的另一个特征函数 x：

\[
K_cx=cx,\qquad x'(\pm1)=1=\frac{x(1)-x(-1)}2.
\]

x 与脚本所有列出的模态正交：与常数、余弦的正交来自奇偶性；与正根正弦的正交来自

\[
\int_{-1}^{1}x\sin(\mu x)\,dx
=\frac{2(\sin\mu-\mu\cos\mu)}{\mu^2}=0.
\]

因此即便无限增加这些列出的模态，程序的二次型仍给 x 零值，不能成为真正的正定谱范数。真实结果为

\[
\|x\|_t^2=c^t\|x\|_{L^2}^2=\frac23c^t>0.
\]

按原代码的基定义作独立最小复现，在 c=3、t=1 时得到约 2.0967×10⁻²⁶，正确值是 2。微小非零值只是浮点积分误差。把这种量代入等距测试，可以在精确层面退化为 0=0；因此此类 PASS 不验证真正的等距。

此外，原代码为 sin(μx) 固定传入范数平方 1，但实际上

\[
\int_{-1}^{1}\sin^2(\mu x)\,dx
=1-\frac{\sin2\mu}{2\mu}
=\frac{\mu^2}{1+\mu^2}\ne1.
\]

**修复：** 补入 `(eigenvalue=c, normsq=2/3, function=x)`；正根正弦项使用 μ²/(1+μ²) 的范数平方；先验证各本征模态及已知多项式的范数，再测试传输等式。原数值报告在此修复前不得作为真实 H^t 范数的验证证据。

## B02：有限谱截断掩盖了实际发散；p4 的失败门槛是 7/2

**来源：** `docs/SL_fractional_left_definite.tex` 的多项式矩界与全实数阶主定理；`scripts/op10_verify_mechanism.py` 的 C 段；`scripts/op10_fractional_window.py` 的 b 段。

对 n≥1，cos(nπx) 在 L²(-1,1) 中的范数为 1。分部积分精确得到

\[
\langle x^2,\cos(n\pi x)\rangle=\frac{4(-1)^n}{(n\pi)^2}.
\]

故谱范数必须满足（允许右端为正无穷）

\[
\|x^2\|_t^2=\frac{2c^t}{9}
 +16\sum_{n=1}^{\infty}\frac{((n\pi)^2+c)^t}{(n\pi)^4}.
\]

级数尾项量级为 n^(2t−4)，于是

\[
x^2\in H^t\ \Longleftrightarrow\ t<\frac32
\qquad(t\ge0).
\]

程序在 t=1.5、1.75、2.5 下报告的有限“范数”不是这些空间中的范数，而是一个发散级数的有限截断。不存在可以用来支持所述命题的有限极限。

独立计算 c=3、t=1.75 的**谱范数平方部分和**：

| 截断 N | 部分和 |
|---:|---:|
| 40 | 110.2266696 |
| 160 | 224.0659342 |
| 640 | 452.2569481 |
| 2560 | 908.9037087 |

这些数值仅展示解析发散结论，不承担证明。

上一轮 p4=x⁴−2x² 的 H⁴ 反例还可以锐化。精确系数为

\[
\langle p_4,\cos(n\pi x)\rangle=-\frac{48(-1)^n}{(n\pi)^4},
\]

从而

\[
\|p_4\|_s^2=\frac{98c^s}{225}
+2304\sum_{n=1}^{\infty}\frac{((n\pi)^2+c)^s}{(n\pi)^8}.
\]

因此对每一个 c>0，

\[
\boxed{p_4\in H^s\ \Longleftrightarrow\ 0\le s<7/2.}
\]

“同一组 p_n 在所有实数阶 H^s 中完备”从 s=7/2 起就不能按原陈述成立，因为族中已有元素不属于空间。这里没有把单个 p4 的归属判据误当成整个族在 s<7/2 的稠密性证明。

## B03：负阶范数与 L² 范数并不等价，原定义下也不完备

**来源：** `docs/SL_fractional_left_definite.tex` 摘要、第 1 节及等距传输定理。

正文由 K_c^(s/2) 在 s<0 时有界，推出 H^s=L² 且范数等价。这只说明作为算子定义域的集合是 L²，并不说明该范数完备，更不说明范数等价。

取 L² 单位特征函数 eₙ=cos(nπx)，λₙ=(nπ)²+c。对 s<0，

\[
\|e_n\|_0=1,\qquad \|e_n\|_s=\lambda_n^{s/2}\to0.
\]

所以不可能有统一正下界 \(\|f\|_s\ge C\|f\|_0\)。进一步，令 f_N=∑_{n=1}^N eₙ，则

\[
\|f_M-f_N\|_{-2}^2=\sum_{n=N+1}^M\lambda_n^{-2}\to0.
\]

它是负阶范数的 Cauchy 序列，但若极限仍在 L²，其每个 eₙ 系数都必须为 1，违反系数平方可和。因此原来作为集合 L² 的这个赋范空间并不完备。

**修复：** 负阶空间应采用 L² 对该弱范数的完备化，或等价的外推/对偶标度，并明确 K_c 在相应标度之间是延拓算子。不能把原 L² 中的无界 K_c 不加说明地作用于每个 H^s 元素。

这不意味着需要先完成负阶理论才能挽救低阶稠密性；见下文“可保留范围”。

## B04：仍被引用的旧求根脚本从第三个正根起失去括根条件

**来源：** `scripts/op10_fractional_window.py`，`mu_roots()`。不要与另一个采用正确括根区间的 `op10_verify_mechanism.py` 混淆。

旧函数使用

\[
\ell_k=(k-1/2)\pi+0.1,\qquad h_k=(k+1/2)\pi-0.1
\]

对 g(μ)=tan μ−μ 二分，却不检查端点异号。右端有

\[
g(h_k)=\cot(0.1)-h_k.
\]

由于 tan(0.1)>0.1，所以 cot(0.1)<10；对 k≥3，h_k≥3.5π−0.1>10.4。因此 g(h_k)<0，而左端同样为负，在这段连续单调分支内没有根。程序只会向错误的右端点逼近。

独立复现：

| 序号 | 原逻辑返回值 | 正确括根所得根 | 返回值的 tan(μ)−μ |
|---:|---:|---:|---:|
| 3 | 10.8955742876 | 10.9041216594 | −0.9289298643 |
| 4 | 14.0371669412 | 14.0661939128 | −4.0705225179 |
| 40 | 127.1345024704 | 127.2266426433 | −117.1678580471 |

**修复：** 使用已知正根所在的 (kπ,kπ+π/2) 分支，构造并检查有效括根区间；验收以根残差和括根有效性为准，而不是只看循环结束。高指标不能长期依赖固定宽度离开极点的右端点。

**影响边界：** 这个旧脚本目前展示的单项式都是偶次，正弦投影在精确算术中因奇偶性为零。因此不能声称每个现有偶模数字都被这处求根错误污染；恰恰是这些测试没有覆盖奇模，掩盖了求根器失效。B02 中偶次单项式的谱级数发散则是独立问题。

## B05：首对比值全局最优性在两篇“自足证明”之间缺失

**来源：** `docs/SL_ratio_proof.tex` 的主定理、平衡相位计算；`docs/SL_mw_lemma_reproof.tex` 的 “self-contained sup-ratio theorem”。

令 ν(R) 按定义为 sup_ρ λ₂/λ₁，令 C(R) 为平衡相位算出的候选闭式。倍指标引理给出的是

\[
\sup_\rho\lambda_{2n}/\lambda_n=\nu(R),
\]

而候选计算只给出

\[
\lambda_2(\rho_*)/\lambda_1(\rho_*)=C(R),
\qquad C(R)\le\nu(R).
\]

两者不能推出 ν(R)≤C(R)。重证文档又把闭式及极大子指回第一篇的平衡相位计算，形成缺失基底最优性步骤的交叉引用。平衡相位算出了一个权重的比值，但没有单独证明所有权重都不超过它。

**判断：** 这是所展示的自足证明链缺口，不是闭式公式的反例；本轮没有声称仓库每个历史研究包都不存在其他证明。

**补足办法：** 明确引用并核对文献中的首对全局极值定理，或者加入独立变分证明。本轮已经另写后一种修复，见 `ratio_first_pair_repair.md`。关键是不预设对称性与平衡相位，而由切换函数 F=u₁²−u₂² 与能量

\[
E=u_1'^2/\lambda_1-u_2'^2/\lambda_2+\rho(u_1^2-u_2^2)
\]

推出 E≡0，再由端点斜率和接口正弦相等推出对称性与平衡宽度。此证明保留原闭式，不把比值问题与谱隙问题混同。

## B06：MW 重证的四处局部问题

**来源：** `docs/SL_mw_lemma_reproof.tex` 的 affine invariance、zero motion、cell extension、minimum truncation。

1. **仿射尺度反向。** L(x)=cx+d 将新区间映到旧区间时，v=y∘L 满足 −v''=c²λ(ρ∘L)v，故新特征值是 c²λ，而不是 λ/c²。比值不变这一后果仍成立。

2. **第二特征函数端点斜率符号错误。** 对 y₂=sin(2π(x+1/2))，两端导数同为 2π，而原文认为异号，并把 t=−y₂'(1/2)/y₂'(−1/2) 写成正数；实际 t=−1。可以直接用真实的有符号传输因子 b_k=y_k'(right)/y_k'(left) 并拼接 b_k^j y_k；若保留旧定义，至少删除错误的 t>0 要求。

3. **最小化截断漏掉共同零点。** c₀=1 并不等价于 z_{2k}<z₀，还可能 z_{2k}=z₀。常密度时这个等号在所有 m≥2 都出现；例如 m=2 时 y₂ 与 y₄ 在中点共同为零。该情形右侧截断的首、次 Dirichlet 特征值正好就是 λ_m、λ_{2m}，直接应用 μ 的定义即可补齐。

4. **零点运动段的 Prüfer 微分式未写完。** 文中有未完成的导数表达式，且密度分段时不能忽略角变量接口。可直接替换为

\[
\frac{dz_j}{d\lambda}
=-\frac{\int_{-1/2}^{z_j}\rho(x)y(x,\lambda)^2\,dx}
{y'(z_j,\lambda)^2}<0,
\]

由参数微分与 Wronskian 得出，避免依赖不完整公式。

这些错误可局部修补；不应仅据此断言倍指标定理错误。分段常数类与分段连续/可测类之间的推广也应显式补上密度逼近及固定编号谱连续性，而不是只更换类的名称。

## B07：K(1)=e/4 中一条终端差分等式错误，但主链可保留

**来源：** `docs/SL_third_order_K1_proof.tex`，Finite backward solutions 一节。

正文定义 d_j=v_j−2v_{j−1}+v_{j−2}，由 v_N=v_{N−1}=0，却写出 d_N=0。实际上

\[
d_N=v_{N-2}=\frac{N}{(N+1)(2N)!}>0.
\]

N=3 时 d₃=1/960，是直接反例。

本轮精确复算了三条阶乘缩放恒等式，并对 N=3,…,30 的每一个适用下标核对了原式 (7) 与直接向后递推，全部一致。N=3 得 μ₀=11/480、μ₁=1/480。后续正项级数及首项/尾项估计的解析推理也能衔接到 j³μ_j*→e/4。

**修复：** 删除 d_N=0 或替换为上述正确值，不需要撤回 e/4 锚点。有限复算只是辅助核对，全范围结论由解析分解及尾项估计承担。

## B08：分数阶文档的 5/8 衰减根说法不符合本递推

**来源：** `docs/SL_fractional_left_definite.tex` 的“衰减模的排除”注。

对正文递推 c u_j=A_j u_{j−1}−B_j u_{j−2}，A_j/j²→4，B_j/j²→4。若存在有限非零比值极限 r_j=u_j/u_{j−1}→L，则

\[
cr_j=A_j-B_j/r_{j-1}
\]

除以 j² 取极限必得 0=4−4/L，故 L=1，不可能为 5/8。这不否定钉住初值 u₀=0,u₁=1 的增长引理；后者已有直接归纳证明，无需该错误说明。

## 可保留的低阶范围：不需要负阶空间

这一段是本轮给出的修复推论，而不是照抄仓库全实数阶主定理。

若采用已独立检查的 H² 稠密性，则立即得到同一稀疏族在 0≤s≤2 的稠密性，特别覆盖 3/2≤s<2。若采用 H³ 主定理，则同理得到 0≤s≤3。

证明如下。对 0≤s≤3，由 λ≥c 有

\[
\|f\|_s\le c^{(s-3)/2}\|f\|_3.
\]

有限谱截断属于 H³，并在 H^s 中逼近任意 H^s 元素，所以 H³ 在 H^s 中稠密。先作有限谱逼近，再用稀疏多项式的 H³ 稠密性逼近该截断，以上连续嵌入即把第二次误差控制到 H^s 中。

因此，可以保留

\[
\overline{\operatorname{span}\{p_n\}}^{H^s}=H^s,
\qquad 0\le s\le3.
\]

本轮不据此评价 3<s<7/2 的整个族稠密性；s≥7/2 的原全族表述则已有 p4 反例。

## 与上一轮的关系及修复次序

上一轮 p4 不属于 H⁴ 的反例与 C1 错误正切上界均被新的精确计算确认，未撤销。此处没有重复声称所有上一轮发现都做了新的完整逐行复验。

建议先撤销失效的分数阶数值证据并修复谱基、根定位与范数定义，再将全实数阶结论收缩为有证明的范围。然后补齐首对比值的全局最优性步骤及 MW 的局部修复，最后更正 K1 的终端差分笔误。不要用统一状态标签代替这些数学修复。

## 复核工件及方法边界

`check_round2.py` 实际运行完成 **34/34 项精确检查**。这里“确认”包括确认原陈述错误及确认局部修复，不表示仓库有 34 个定理全部通过。精确检查覆盖符号分解、有理数界、低阶 Fourier 系数、有限向后递推及常系数反例。

`round2_results.json` 另保存浮点最小复现：漏模态范数、旧求根器残差、发散谱级数的部分和。它们和精确检查分区登记，不作为无限维或全参数证明。

阅读顺序：本报告 → 独立首对比值修复证明 → 检查脚本与 JSON。所有生成文件都是审计附件，不是对仓库原文件的修改，也未做形式化机器证明。
