# Sturm–Liouville 仓库第十一轮审计

日期：2026-09-25。

固定仓库：`Zhongshan-Big-Jun/Sturm-Liouville-theory-research`。

固定提交：`4f6b35cbdc433d2d319cd3e483b6dbf0af3f9d87`，2026-09-25 08:47:44 UTC，`Fix indexed spectral roots and geometric reflection seeds`。

结束前重新读取默认分支提交，仍为上述固定提交。

本轮只读审查仓库；没有修改或提交远端文件，没有把此前讲义 PDF 当作仓库当前版本。

## 总结

上一轮的低根枚举和反射种子问题得到实质修复。当前相位引擎在旧高反差反例上返回正确的低根前缀；反射种子源码已使用互补投影并检查转换后的实际几何。

继续检查共享 Jacobian 诊断发现两项问题：

| 编号 | 类型 | 结论 |
|---|---|---|
| R11-01 | 实际程序的矩阵分块错误 | `sym_antisym_decomp` 对反对合 Jacobian 提取了应为零的两个对角块，不能作为其非退化扇区块 |
| R11-02 | 数值差分路径被坐标修复改变 | `jac_fd` 用固定步长扰动自由块宽，再通过会裁剪/归一化的 `widths_to_z`；窄块时不是声称的中心差分，且未拒绝 |

这两项不否定当前工具卡中的正确反对合恒等式，不证明默认 R=4 驻点表错误，不撤回原完备性或最新左定文献吸收的主结果。

本轮另给出 `cofinite_all_orders_proof.md`：把既有域字典和高阶核心相接，得到整个 $0\le s<7/2$ 窗口的余有限闭包分类，包括 $s=1/2,3/2,5/2$。这是本轮新增推导，尚未写回仓库或获得另一位审稿人的独立验收。

## 1. 第十轮修订复查

### 1.1 当前实际求根模块

读取并恢复 `scripts/_sl_prufer.py` 的原字节，Git blob SHA 为
`3800e210282cd1bf19f1523609b75b9498135754`，一致后实际导入执行。

其逐项求根使用保留整圈信息的块内相位和正坐标转换，对每个 $n\pi$ 单独二分。代码不再依赖世俗函数的固定网格变号，不把 `npts` 用于确定谱序号。

对于块长 $(.02,.96,.02)$、密度 $(10000,1,10000)$，实际返回的前四频率为

```
0.7834240122391394
0.7978076544144250
2.3457078475446362
2.3585404210821923
```

请求 3、6、61 个模态时前缀一致。独立以 70 位物理状态 $(y,y')$ 传播并枚举每块 $A\cos(\omega\sqrt\rho\,t)+B\sin(\omega\sqrt\rho\,t)$ 的零点，在根间检验得到计数 $0,1,2,3,4,5$。这不是第二份相位实现，但仍只是有限高精度核对，不是全参数区间认证。

当前数学契约 `research/artifacts/proof-audit-round10-20260925/analytic-repair.md` 的相位单调性、端点、比较括区和局部坐标转换在本轮阅读中没有发现新缺口。高精度消费者仍需保持谱指标；本轮未执行其完整 CLI。

### 1.2 反射种子

阅读 `scripts/reflection_seeds.py` 的投影、步长回退和实际转换后校验。几何反射为 $x\mapsto1-Px$，保持投影 $(I-P)/2$、破缺投影 $(I+P)/2$ 正确。代码显式处理零投影、不对称基点、不可行宽度、不可分辨扰动和转换后的方向变化。

本轮没有运行整个种子生成器的全部测试和侦察主流程；这里的肯定判断是对已读公式与控制流程，不是复述原报告的全部执行回执。

## 2. R11-01：把应为零的对角块当作 Jacobian 的扇区块

定位：`scripts/_gapn2_jacobian_probe.py`，文件说明、`sym_antisym_decomp` 与 `main` 打印 `det A`、`det B` 的部分。

该完整文件的 Git blob 为 `442f8e52572ccd80ecc6ddcfe648b9358c45bb6c`。本轮恢复并校验原字节，使用 AST 执行未改动的 `sym_antisym_decomp` 函数体。

令 $F_i(x)=f_\rho(x_i)/\lambda_{n+1}$，$P$ 为坐标反转。当前工具卡 `tools/band-selfconsistency-equivariance.md` 已正确给出
\[
F(1-Px)=PF(x).
\]
对称点的 Jacobian $J=D_xF$ 因而满足
\[
JP=-PJ.
\]
将输入、输出都用相同的和/差坐标变换 $S$ 后，应有
\[
SJS^{-1}=\begin{pmatrix}0&C\\D&0\end{pmatrix},\qquad
\det J=(-1)^n\det C\det D.
\]
源码却返回 `M[:n,:n]` 和 `M[n:,n:]`，即应当为零的两个对角块。文件头“commutes with reflection”也与实际模型和当前工具卡相反。

### 实际 SL 反例

取 $n=2,R=1$，归一化残差为
\[
F_i=2\left[\frac49\sin^2(2\pi x_i)-\sin^2(3\pi x_i)\right].
\]
它对接口只有逐分量位置依赖。令
\[
t_\pm=(11\pm2\sqrt{10})/36,
\quad x_1=\pi^{-1}\arccos\sqrt{t_+},\quad x_2=\pi^{-1}\arccos\sqrt{t_-},
\quad x_3=1-x_2,\quad x_4=1-x_1.
\]
此时 $F=0$，且
\[
J=\operatorname{diag}(d_1,d_2,-d_2,-d_1),\quad
 d_i=\frac{16\pi}{9}\sin(4\pi x_i)-6\pi\sin(6\pi x_i).
\]
精确计算给出
\[
\det J=\frac{7030400000\pi^4}{4782969}>0.
\]
数值约为 $143179.86873957375$，而执行原分块函数得到两个全零矩阵。这是实际模型中的非退化 Jacobian，不是任意造出的矩阵反例。

### 最小修复

对 **J** 返回交叉块 `M[:n,n:]` 和 `M[n:,:n]`，检查对角块残差，并采用正确行列式公式。将“坐标反转偶/奇”和“物理反射保持/破缺”命名清楚。

不要在所有矩阵上统一交换分块位置。标量谱隙的 Hessian，以及带跳量因子的适当对称化矩阵，可以与反转交换并具有块对角结构；它们与残差 Jacobian 不是同一个对象。

影响仅直接落在该程序显示的块及其下游解释。工具卡中的正确解析反对合恒等式应保留；本轮未逐一追查所有历史数值输出。

## 3. R11-02：默认中心差分经过裁剪后不再是原来的方向

定位：同一文件 `jac_fd`，结合当前 `scripts/_gapn2_symmetry_recon.py` 的 `Recon.widths_to_z`。

`jac_fd` 对前 $2n$ 个自由块宽作 $\pm h$ 扰动，用最后一块保持总长。随后 `widths_to_z` 先执行 `np.clip(w,2e-7,1-2e-7)`，再除以总和。默认 $h=10^{-6}$ 未根据实际块宽选择，也没有核验转换后的差分端点仍等于目标端点。

### 隔离谱误差后的反例

仍用 $n=2,R=1$ 的解析残差，取
\[
x=(0.25,0.2500003,0.7499997,0.75),
\]
\[
w=(0.25,3\times10^{-7},0.4999994,3\times10^{-7},0.25).
\]
基点严格可行，每块大于 $2\times10^{-7}$，基点本身不发生裁剪。真实 Jacobian 为对角矩阵，第一项为 $6\pi$。

本轮将当前未改动的 `jac_fd` 函数体，作用于使用上述解析残差和当前两条坐标转换公式的适配器，以排除求根、谱截断、求积和本征函数归一化误差。结果：

| 步长 | 相对真实 Jacobian 的最大元素绝对误差 |
|---|---:|
| 默认 $10^{-6}$ | 6.361733922377599 |
| $10^{-8}$ | 约 $3.61\times10^{-7}$ |

默认输出产生大量本应为零的非对角项，例如约 $6.3617339$。更小步长的对照不是通用误差认证，只说明本例大误差来自差分端点的裁剪。

精确有理解释：第二块负向扰动后为 $-7\times10^{-7}$，被改为 $2\times10^{-7}$。总长因此成为 $1+9\times10^{-7}$，归一化又把原本应保持不动的第一接口从
\[
\frac14\quad\text{改成}\quad\frac{1/4}{1+9\times10^{-7}}.
\]
所以这已不是声称的坐标方向上的差分。

### 修复与边界

应以两侧都可行且不触发修复的步长计算；最好直接在接口坐标构造两端，再检查转换前后位置。不可行或不可分辨时拒绝，而不是剪裁后继续沿用原分母。可以用两档合法步长交叉检查，但这不自动成为严格区间 Hessian 认证。

当前二阶变分程序的 P3 仍调用 `jac_fd`。这说明它不是完全无调用的陈旧代码。然而本反例不是默认 R=4 的驻点，也不证明本轮报告中的默认样本已发生此问题；实际受影响的参数需要单独追踪。

新反射种子生成器已经验证转换后的几何，不能因此被一并判错。这里是同类保护尚未传播到 Jacobian 差分端点。

## 4. 左定主线：一项建设性补证

当前 `tools/krein-s3-cofinite-three-traces.md` 只声明 $s=3$，并明确不覆盖 $s=5/2$；当前分数域字典则已给出中心迹连续性的严格门槛。

附录 `cofinite_all_orders_proof.md` 把它们与四阶高消失多项式核心接通，证明对任意余有限 $N\subset\mathcal D$：
\[
\overline{\operatorname{span}\{p_n:n\in N\}}^{\mathcal H_c^s}
=\bigcap_{j\in I(s)\setminus N}\ker\tau_j,
\]
其中 $\tau_0f=f(0)$、$\tau_1f=f'(0)$、$\tau_4f=f''(0)$，并且

| 阶数 | 完整稠密所必须保留的指标 $I(s)$ |
|---|---|
| $0\le s\le1/2$ | 无 |
| $1/2<s\le3/2$ | $0$ |
| $3/2<s\le5/2$ | $0,1$ |
| $5/2<s<7/2$ | $0,1,4$ |

论证不是把三阶的迹条件直接外推。先在四阶域中建立任意高中心消失阶的相容多项式核心，再把任何连续零化泛函限制为四个中心迹的组合；最后用固定内部支撑的 Fourier 对数序列排除不连续的最高迹。它因此同时处理三个临界值的充分性。

该补证也推出，在整个原族成员窗口内，删除 $p_6$ 后仍稠密，故原族及其逐项非零缩放都不是 Schauder／Riesz 基。它不针对 Legendre 相容替代系统，不处理任意无限删项或一般约束筛选，不给出误差速度。

这是本轮新增推导，不是已经合并、正式发表、Lean 形式化或外部独立验收的结果。全文给出了精确输入及完整证明，便于下一轮独立复查。

## 5. 本轮执行与未执行范围

实际执行：

- 两个完整源码文件的 Git blob 身份校验；当前 `_sl_prufer.py` 实际导入和回归。
- 当前 `jac_fd` 与 `sym_antisym_decomp` 的未改动 AST 函数体；真实常密度谱模型上的解析校准与窄块反例。
- 全指标边界修正行列式、R=1 根式 determinant 的符号核对，以及有限中心迹／边界矩阵检查。
- 独立程序正常模式与 `python -O`，各 20 个具名检查组全部得到确认。确认包括确认仓库程序失败，不等于 20 个定理通过机器认证。

未执行：完整侦察和二阶变分 CLI、全部反射种子回归、全仓库测试套件、Lean、完整 M3/KP、全部历史证书、全部文献全文审计。本轮扩查还阅读了精确开关证明的相关段落和明确撤回的旧高阶正交系文件；没有把撤回稿的旧错误当作新错误。

容器直连 GitHub 在本轮出现 DNS 失败；因此通过 GitHub 连接器读取源码，再按原文本恢复两个小文件并核对 Git blob。没有把手工重写的近似实现冒充原字节文件。常密度适配器独立撰写，作用是隔离谱数值误差，其范围已在上述第 3 节说明。

## 6. 可复现文件

运行：

```sh
python checks.py
python -O checks.py
```

依赖：Python、NumPy、mpmath、SymPy；只写本包的 `evidence/`，不需要网络、不修改远端仓库。

`evidence/checks_normal.json`、`evidence/checks_optimized.json` 记录实际数值与范围；对应日志在同一目录。`source/` 的两个文件按固定提交逐字节校验；`source_manifest.json` 记录身份和获取方式。
