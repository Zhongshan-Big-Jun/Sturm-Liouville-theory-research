# 第十二轮仓库审计

## 固定范围

仓库：Zhongshan-Big-Jun/Sturm-Liouville-theory-research。

固定提交：`be7c0912849a50a46869773eab4b3a2ec655ce88`，提交时间 `2026-09-25T19:02:49Z`。开始和结束前均通过当前 GitHub API 核对该 HEAD。审查日期：2026-09-26。本轮只读，没有远端写入。

## 总评

第十一轮的 Jacobian 交叉块、真实差分端点保护和全部成员窗口内的余有限闭包证明，所核对的关键链条能够成立。新发现集中在独立的半区间谱/Green 诊断：它没有调用已修复的全区间索引引擎，低谱枚举仍会随截断数改变；继而去极点的谱和可能删错项，实际产生无穷大。另外，旧工具卡把 K' 的奇块写成原 K 的奇块，已有的后续脚本却正确地区分了两者。

这些问题不否定已修复的左定完备性与余有限闭包结果；也没有证据据此宣布默认 R=4 的所有历史表错误。

## 已修复事项复查

### Jacobian

当前 `scripts/_gapn2_jacobian_probe.py` 的 `jacobian_cross_blocks` 验证 JP=-PJ 并返回两个交叉块；输入对易矩阵会拒绝。本轮执行了这些源函数的独立摘录，用常密度 n=2 的解析 Jacobian 和一般反对易矩阵交叉检查。

`jac_fd` 现在直接在独立接口坐标构造中心差分两端，按相邻块宽缩步并检查转换后真实位移。在上轮窄块 `(0.25,3e-7,0.4999994,3e-7,0.25)` 的解析残差适配器上，本轮最大 Jacobian 元素误差约 `1.2583e-8`；采用的四列步长均约 `7.5e-8`。这是有限浮点试验，不是通用导数误差界。

### 全窗口余有限闭包

阅读 `research/artifacts/proof-audit-round11-20260926/cofinite-author/cofinite-all-orders.md` 的算子基础、成员性、二次 K 泛函、局部乘子、四阶核心、连续零化泛函压缩、复迹对数序列及闭包步骤。未发现关键论证缺口。

特别是，当前证明不是简单把 H3 结论插值：它独立建立局部双向范数控制，将零化泛函在稠密 H4 子空间压缩为四个中心迹，再排除最高不连续迹，包括临界值。s=1/2、3/2、5/2 属于较少迹的一侧，这个结论可以保留。

本轮符号检查涵盖两个全参数边界行列式、低阶迹表和 K 泛函逐坐标极小式；无穷维定理由实际阅读的解析证明承担，不由这些有限检查承担。

## R12-01：半区间自己的扫描求根器仍然错认谱序号

位置：`scripts/_gapn2_half_problem_probe.py`，`_secular_all`、`half_spectrum`。

`half_spectrum` 自己在 mu 上做均匀扫描，不调用 `_sl_prufer.indexed_roots`；修改全区间共享引擎并没有改变它。N 同时控制搜索上界和网格。增加 N 可能使低频区间更粗。

反例：半区间 `(0,1/2)`，常密度100。

- D真实谱：mu_j = j² pi²/25。
- N真实谱：mu_j = (j-1/2)² pi²/25。

执行同一个源函数，D情形：

| N | 前缀的真实序号 | 首个返回值 |
|---|---|---|
|4|1,2,3,4|0.3947841760435744|
|80|3,4,5,6,7|3.5530575843921692|
|160|3,6,7,8,9|3.5530575843921692|

N情形：N=80 的首项是第3模态，N=160 的首项是第5模态。

用精确有理 π 包络和实际二进制网格端点，另行证明 N=80 的第一格恰装下前两个 D 根；两端同号，所以扫描必然漏掉它们。此证明不依赖另一个数值求根器。

处置：对 D 和 N 分别使用 n*pi 与 (n-1/2)*pi 的提升相位目标，保持谱序号；检验不同 N 的前缀一致性。不能只增加扫描密度、二分次数或有效位数。

## R12-02：重新枚举再按数组下标去极点，实际留下零分母

位置：同文件 `_spectral_green` 和 main 的 C2 调用。

main 从2N枚举取得目标值，辅助函数却用N重新枚举，只按 pole_idx 删除一项。上项错误使两个数组不嵌套，pole_idx 不再对应同一特征值。

常密度100、N=80的例子中，2N列表的 `muN[1]` 是真实第6模态：`11.942221325318123`。在N列表里它位于下标3，程序却删下标1。执行 `_spectral_green(..., pole_idx=1, bc='N', x=y=0.1,N=80)`，实际返回 `inf`，警告为 `divide by zero encountered in scalar divide`。

这是正确去极点之前的程序故障，不是约化 Green 核的发散证据。它与 R12-01 具有共同根因，不能虚构成完全独立的数学错误。

处置：在正确枚举基础上共享同一谱表，绑定边界类型、谱指标和目标值，检查去掉的确是目标极点。N/2N截断或 Richardson 外推不能修复错位谱和。

范围：复现使用的是源函数摘录和与main相同的调用关系，不是运行整个驻点CLI。默认R4和全部旧Green文件尚未逐项重跑，须按调用和参数追踪。

## R12-03：旧 Green 卡片把 E Ke E 写成 Ko

位置：`tools/green-half-inertia.md` 的“奇扇区定性判据（历史 Green 表达式保留）”。

它以 `K=diag(1/s)J` 定义原矩阵，保留一条称为 Ko 的异奇偶类Green闭式。令 S=diag(1,-1,...)，K'=SKS，Be/Bo为标准和/差镜基底，E为左半交替符号矩阵。恒有

`Bo^T K' Bo = E (Be^T K Be) E = E Ke E`。

在n=2时，所保留的异奇偶Green右端恰是 K' 的奇块，所以不是原 Ko。当前 `_gapn2_half_problem_probe.py` 末尾已经使用 `Kp_odd` 命名并与 `E Ke E` 比较，说明同仓库里已有正确对象区分；旧卡片未同步。

独立65位传递和隐式求导的 R4 SUP 检查：旧右端与原 Ko 最大元素差约5.06042，与 E Ke E 误差约4.75e-65。另用物理解逐块零点计数核对模态2、3。

此处必须保留状态区别：工具卡已经声明旧Green推导未经本轮全面复核，本发现是进一步定位这段历史表达式的具体问题，不是宣称刚获独立验收的全窗口闭包被推翻。

处置：改正左端名称及相应的扇区解释；需要原 Ko 时保留本征极点的约化核和秩一项。不得因为两个全矩阵合同就把它们的同名扇区当作同一个矩阵。正确n=2公式与推导见 analytic_repairs.md。

## 可以保留的 Green 计算

`_prims_9` 的九个初等原函数均通过符号求导与零端点值检查。`Gt=B-uP` 的源项、跳量、边界条件及加权正交结构未发现错误。不能因谱枚举和扇区标注错误而一并否定这些部分。

## 执行与局限

独立 checks.py 在普通模式和 `python -O` 下分别确认34个具名检查，包含“确认错误”的检查，不是34个定理获机器认证。实际环境和每项结果保存在 evidence/。

源函数执行采用 GitHub 返回内容的函数摘录，加入必要import和来源头；没有下载并验证整个原源码文件字节。source_manifest.json 区分连接器报告的上游Git blob与本地摘录的SHA-256。没有调用原CLI、没有修改远端、没有执行作者整个测试套件、Lean、完整M3/KP、全部历史Green扫描或全参数区间认证。

独立Green程序是本轮重新写的数学实现，不是作者程序。65位数值结果不代替解析恒等式或驻点区间认证。

建议修复顺序：先把半区间D/N枚举和极点身份统一到索引体系，再同步Ko/K'对象约定，最后针对受影响历史证据重算。上一轮已经修好的差分路径与全窗口闭包应保留。
