# Sturm–Liouville 仓库第六轮证明审计

日期：2026-09-21。
仓库：`Zhongshan-Big-Jun/Sturm-Liouville-theory-research`。
固定版本：`92d46e99e36cc0a74e8f7bb13430749c222ee54f`。
该提交的作者时间为 2026-09-21 05:52:02 UTC，父提交为 `c0b36b90004cffb0552c9fa9f229e1f7cec8a706`。

## 结论

第五轮新增的第二左定空间两迹闭包分类，在本轮核对的算子基础、Green 代表元、完整尾部逼近、余有限闭包和复子空间分类中，未发现实质性证明缺口。

本轮在其一般完备性上游找到一个仍记录为 STRICT/PASS 的错误：从全部多项式的投影稠密性，错误推出原稀疏族的投影稠密性。该错误会使随后的“只检查被排除成员投影”的充要判据也失效。此外，一个旧有限矩充分准则具有互相矛盾的假设，另一条约束解释混淆非零检测与分别钉零。

同时，本轮给出建设性补证，将原稀疏族的非负阶稠密范围从当前仓库记录的 0<=s<=3 推进到完整的 0<=s<7/2。证明先核对所有原成员的谱系数，再利用四阶边界相容多项式的稠密性；不是从三阶稠密性直接向上外推。

未改动仓库，未提交 commit。新补证尚未被仓库接收，也未获另一独立审稿人或 Lean 的认证。

## 1. 本轮范围

直接核对当前版本的：

- `docs/SL_cofinite_left_definite.tex` 的主要解析链；
- `docs/SL_fractional_left_definite.tex` 的谱系、成员阈值与原开放窗口；
- DensBC 2026-08-14 和 2026-08-16 两个 run 的相关候选定理，以及后一 run 的原审计；
- `tools/constrained-denseness-runs.md` 的当前复用范围；
- `research_map.md` 的 A1/A4/A7 关系；
- A7 四阶图核心的相应证明段。

没有重审 M3/KP、全部间距与比值证明、完整 Lean 工程、全部历史区间证书或全部诊断程序。不能把“本轮未发现缺口”理解为全仓库认证。

## 2. 第五轮修订的复查

### 2.1 算子和拓扑

新文稿从能量恒等式推出 K_c>=cI，并补足普通 H2 范数控制。显式的双曲函数边界修正证明满射，自伴性的伴随定义论证可衔接。常数只针对固定 c>0，没有误称退化参数下一致。

### 2.2 Green 代表元

两个代表元分别承担 f(0)、f'(0)，其界面跳量为 (0,-1)、(1,0)，外端点条件能够消去边界项。复内积下的共轭位置和低阶矩归一化正确。本轮以符号微分、符号积分独立确认了这些恒等式。

### 2.3 尾部逼近和闭包

局部截断估计保留了趋零的局部范数 ||f''||_{L2(-2delta,2delta)}，不是仅给一个全区间常数界。随后固定 delta 作多项式逼近，再修正两个端点残差；两个极限的顺序明确。

余有限所选指标集的闭包、实际保留集来自闭空间时的二维迹分类，以及斜约束的缺陷均能从该尾部定理推出。本轮没有发现这些步骤的实质性错误。

## 3. R6-F01：投影稀疏族定理及其推论为假

**定位：**
`runs/rigorous-open-math-research/R-20260816T000000Z-densbc-o1/candidate_proof.md`
第 1 节 Theorem 1、Corollary 1.1。

**传播：** 同 run 的 `audit_report.md` 仍将 Theorem 1 记为 PASS；当前 `research_map.md` 将这一 run 的结构定理作为 A4 上游。旧源在新提交中保留并不是错误本身；问题在于其过强结论仍作为已证结构被引用。

### 问题

P_V(Pi) 在 V 中稠密是正确的。但原文以 span{p_n}=Pi 为理由，声称 span{P_V(p_n)} 也总在 V 中稠密。正确代数关系是

    Pi = span({p_n} union {x^2,x^3}),

而不是 span{p_n}=Pi。

### 非平凡有限约束反例

取对角空间 H_beta，beta=2，满足

    <x^j,x^k> = delta_jk (k+1)^4,
    V = ker M0.

令

    w = sum_{m>=1} m/(2m+1)^4 x^(2m).

则

    0 < ||w||^2 = sum m^2/(2m+1)^4 < infinity,
    M0=M1=0, M_(2m)=m, M_(2m+1)=0.

故 w 属于 V，且正交于全部 p_n。由于 P_Vp0=0，其余 P_Vp_n=p_n，所以 w 也正交于全部投影后的稀疏族。w 非零且 <w,x^2>=1，证明投影稀疏族不稠密。

实际被排除的只有 p0，它的投影为0，所以 Corollary 1.1 右侧的全部条件成立，但其左侧不成立。故该充要判据也被反证。

### 最小修复

保留全部多项式投影稠密性；投影稀疏族需增加原族在 H 稠密的假设，或采用精确条件 V cap (span{p_n})^perp={0}。

不增加假设的构造性修复是补回 P_Vx^2、P_Vx^3。原筛选族判据也必须要求这两个投影在筛选族闭包中，而不是只检查被排除 p_n 的投影。

不自动否定后续直接通过矩可表示性证明的对子类判据，也不影响已经另证全族稠密性的具体 Krein 低阶投影结果。应按实际依赖区分。

完整反例与修复见 `projection_and_moment_repairs.md` 第1–4节。

## 4. R6-F02：旧 Theorem F 的假设使它没有适用实例

**定位：**
`runs/rigorous-open-math-research/R-20260814T070000Z-densbc-3F8A2C/candidate_proof.md`
第5节 Theorem F；Theorem G 又交叉引用它的低阶条件。
当前 `tools/constrained-denseness-runs.md` 仍将 F/G 概括为 STRICT 充分准则。

### 问题

F 一方面要求 V 包含无限的完整稀疏尾部，因此 V 无限维；另一方面要求有限个低阶测试和 M2=M3=0 的联合核在 V 内只有0。这相当于从无限维 V 向一个有限维坐标空间的线性映射为单射，不能成立。

精确地，若共有 r 个低阶测试，在 V 中任选 r+3 个独立尾部多项式，r+2 个线性条件在该子空间中必有非零解。

**分类：这是空洞的条件定理，而不是“假设成立、结论失败”的反例。** 不应将它当作可以实际应用的尾部充分准则。

### 修复

有限测试应作用于“经过全部尾部正交条件后剩余的有限维障碍空间”，而不是整个 V。

在 ||x^k||<=C(k+1)^beta、beta<1 下，令 L=2m0-2，则尾部的正交补精确等于

    Z_L = {w: M_k(w)=0 for every k>=L},  dim Z_L<=L.

因此，应在 V cap Z_L 内检查低阶保留测试是否具有零核。这是有适用实例的正确版本，证明见修复附录第5–6节。

## 5. R6-F03：非零检测不等于分别消除两个矩

**定位：** 同一2026-08-14候选稿第6节 Theorem H 的解释段。

它把“约束表示元对 x2、x3 非零”与“x2、x3 属于 Vperp”混同。
取 H0 对角空间、表示元 v=x2+x3。它对两个单项式的配对都为1，但 w=x2-x3 属于 ker<.,v>，而 M2(w)=1、M3(w)=-1，并未分别为零。

应改为

    span{x2,x3} subset Vperp = span{v_1,...,v_r}.

这一错误解释不推翻 master criterion V cap Qperp={0} 本身。修复附录第7节给出完整定位。

## 6. R6-C01：分数窗口可以给出完整补证

**当前状态：** `docs/SL_fractional_left_definite.tex` 与 `research_map.md` A1 仍将 3<s<7/2 标为未判定。已有 A7 的整数高阶多项式图核心与这一窗口尚未接通。

**本轮结果：** 对每个 c>0、0<=s<7/2，原稀疏多项式族在真正的 Hc^s 中稠密；每个命名非仿射成员的精确阈值都是 s<7/2。

证明关键：

1. 任意多项式 g 的余弦、Krein 正弦谱系数都是 O(k^-2)。
2. 若 p 属于第一层算子域，自伴性使其谱系数再除以特征值，成为 O(k^-4)，故全部原成员属于 s<7/2。
3. 对每个非仿射原成员，首项边界系数分别为 4m(4m-5)、4m(4m-3)，非零，故在 s>=7/2 发散。
4. C[x] cap Hc^4 在 Hc^4 中稠密，而且包含于原稀疏族的有限线性包。
5. 从 Hc^s 先作谱截断进入 Hc^4，再用四阶相容多项式逼近，最后通过连续嵌入控制 Hc^s 误差。

附录 `fractional_window_completion.md` 还单独证明了所需四阶图核心，使用一个行列式为15360的四迹修正矩阵。因而该推导不需要以整个 A7 的审计标签为黑箱。

这是本轮新增定理推导，而不是仓库已经接受的状态。接入后才能更新 A1。不得将其外推到一般受约束空间、无限删项、稳定数值基或任意其他边界条件。

## 7. 实际执行与证据边界

实际执行 `check_round6.py`，正常模式和 `python -O` 均退出0，21个具名检查组得到确认。检查类型分别标记为符号恒等式、符号积分、有限有理界、有限精确实例和矩阵秩。

其中“确认”包含确认反例，不等于21个仓库定理通过认证。所有无穷维和全指标结论由两个附录中的解析论证承担。

本轮没有运行仓库原程序或全套测试，也没有 Lean 编译、PDF重建或排版审查。原始材料通过 GitHub 连接器读取；普通容器网络不能解析 raw 主机，因此不声称已经取得原文件本地字节副本或验证其本地blob一致性。来源清单记录的是连接器返回的路径、版本和blob身份。

## 8. 建议修复顺序

1. 对 O1 Theorem 1 的稀疏投影“特别地”和 Corollary 1.1 建立勘误，停止无条件复用；保留真命题 P_V(Pi) 稠密。
2. 用有限维尾部障碍空间重写 F，并修复 G 的不明确交叉引用和 H 的表示元条件。
3. 独立复核窗口补证后，将原族范围写成 0<=s<7/2；保留高阶成员障碍及域交稠密性的区别。
4. 保留第五轮已经修好的两迹分类，不要求因上游抽象投影错误而撤回它；它有自己的逼近证明，并不依赖这条假“特别地”。

本轮总体判断：新的具体主链保持有效；一般理论的两处旧复用接口仍需修正，同时原族的分数阶边界已经有一条完整、可单独复核的补证。
