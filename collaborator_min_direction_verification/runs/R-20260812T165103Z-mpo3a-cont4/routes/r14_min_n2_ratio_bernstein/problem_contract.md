# Problem contract: general-mu, n=2 minimum interface

- Route status: `RIGOROUS_PARTIAL_RESULT`.
- Canonical Blueprint SHA-256:
  `0120d1fb32af1a30449575995efccb6d1afcce416ee671ad00a5f296400fd799`.
- Evidence inventory SHA-256:
  `b6286574edbcb70ad22e5c6758a81f00dd01572c0764b8816be23cb6b166fb6f`.
- Accepted upstream route:
  `../r11_min_n2_general_mu/derivation.md`.
- Parameters: arbitrary `mu>1`, the `n=2` positive-to-negative local
  interface, and contrast variable `r=sqrt(R)>1` in its strict physical
  interval.
- Target: prove the local numerator `Phi>0`, which is the remaining local
  sign needed by the minimum-interface twist route.
- Exact scope achieved here:
  1. the putative lower endpoint `r_A` is always negative;
  2. `Phi>0` is proved on the full half-domain `F_+F_- >= 1`;
  3. on `F_+F_-<1`, an exact phase-ratio monotonicity theorem proves the
     bridge inequality `w>x_+` and reduces the remaining task to four
     explicit Bernstein coefficient inequalities.
- Explicit non-claim: the four remaining coefficients are not signed in
  this route.  Therefore this route does not prove the full general-`mu`
  interface theorem, the global minimum twist theorem, or O3a.
- Numerical searches are discovery-only and are not premises.
- No canonical file or proposal is modified by this route.

