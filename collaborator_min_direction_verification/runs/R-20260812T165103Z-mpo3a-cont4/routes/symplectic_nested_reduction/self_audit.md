# Self-audit — symplectic nested reduction

## Definition audit

The state order and signed symplectic form agree with the accepted relay
Hamiltonian package. `eta` is a hybrid fixed-time sensitivity, while the
indexed zero-time derivatives are introduced separately. Reflection uses one
common normalization factor `abs(p)` for both modes, so the integral ratio is
indeed invariant.

## Logic audit

1. Homogeneity gives the exact scaling Jacobi field `xi=z`, including at
   moving relay events.
2. Initial symplectic pairing with the `q` field vanishes.
3. At the double Dirichlet terminal point the preserved pairing is exactly
   `-p eta_U+r eta_V=0`.
4. The implicit zero-time formulas and terminal energy then give (3.3).
5. For `q>1`, `p` and `r` are nonzero and (3.4) is an equivalence.
6. Reflection energy algebra gives (4.2); fixedness gives (4.4).

## Boundary/adversarial audit

The note does not infer global monotonicity from local nondegeneracy, does not
infer uniqueness from reversibility, and does not infer a proof from the
finite observations that motivated the route. The remaining statements in
Section 5 are obligations, not conclusions.

Self-audit verdict: PASS as an exact partial reduction, subject to independent
review before any canonical promotion.

