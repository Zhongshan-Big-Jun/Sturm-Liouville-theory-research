# Append-only research ledger: MIN-REFL-C2-G

- Bound the route to the continuation problem contract and independently
  hash-matched/replayed the assigned forest report and exact checker.
- Reconstructed a general-`mu` positive--negative interface directly from
  both momentum equations.  The resulting amplitudes are Möbius functions
  of the one shared `r=sqrt(R)`.
- Time-reversed the left interface of the middle positive cell and derived
  the exact quadratic shared-contrast compatibility equation.  This blocks
  reuse of the relaxed separate-contrast witness.
- Compared the physical `b=0` boundary with the positive-cell `a=1`
  crossing.  Exact factorization isolates `Xi`; `Xi>=0` forces strict
  contraction.  Hence every physical `n=3` word needs `Xi<0` on at least
  one side, and a reflected word needs `Xi<0` in its reduced interface.
- Reconstructed all three blocks of `P^(-1)f` from
  `f=P gamma-C^T v`.  Derived the four-margin determinant identity.
- Audited the additional total-charge identity
  `Q=f^T P^(-1)f-gamma^T f`: both terms are positive under the trusted
  forcing, but their difference is not oriented.
- Localized the first coupled response deficit to
  `D_mid=s_2^2v_1v_2-E_2F_2`, the negative of the determinant of a loaded
  central Robin matrix after positive scaling.  Shared contrast has not yet
  oriented its two boundary load ratios.
- Removed numerical discovery output from the frozen checker.  The retained
  certificate is exact symbolic algebra only.

