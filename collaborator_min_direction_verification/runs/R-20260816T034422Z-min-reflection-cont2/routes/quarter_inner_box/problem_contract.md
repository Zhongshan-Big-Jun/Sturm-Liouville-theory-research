# C2-O problem contract: uniform-quarter inner cube

Target only the exact dyadic inner cube
`(k,t,y) in [1/64,63/64]^3` on the retained subset `g<1,rB>1`.
The frozen R17 evaluator is used with the stronger directed target

```text
g*Knew*cp^4/4 - Pplus*Nhat_i > 0,  i=1,...,4.
```

A complete run is a finite conditional coefficient certificate.  It does
not cover any boundary collar, reprove the physical bridge, or establish
global reflection.  An incomplete run is retained only as a route-finding
artifact and cannot propagate.
